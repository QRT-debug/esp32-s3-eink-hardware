# 原理图绘制清单

> 目标：按模块分块绘制，避免一次性放完所有元件后难以检查。每完成一阶段在 EasyEDA Pro 中按 `Ctrl+S` 保存，然后让 agent 用 `easyeda sch read` 核对活体网表。

## 图纸规范（任何一页、任何一张示意图都适用）

### 呈现格式

- **一整张完整成品图**：电源轨、地轨、全部无源器件与 IC 用真实走线连在同一张图上，像别人画好的原理图，不做「IC 接法」+「元件清单」的分块。
- **每个引脚单独标出脚号与名称**，重复脚（如 VIN 10/11、VOUT 4/5、L1 8/9）也逐个画出后再并接。
- **标签放在无走线的空白带**（例如电轨上方的横带），绝不压在线上；同组元件可合并成一个标签。
- 按网络把引脚分组摆位（接同一电轨的脚放同一侧）——这样可以做到**全图零走线交叉**。

### 交付前必查：连通性五条

| # | 检查项 | 说明 |
| --- | --- | --- |
| 1 | **旗标贴边坐标 == 轨端点坐标** | 网络标号的贴附边必须与电轨端点严格相等，差 1px 就是断的 |
| 2 | **轨两端必须落在连接点上** | 每条轨的起止都要停在某个元件引线或接点上，不留任何悬空线头 |
| 3 | **T 型相接必须打接点圆点** | 十字交叉不打点 = 电气上不相连；EDA 一般会自动加点，但前提是端点严丝合缝 |
| 4 | **不接的脚用标准 NC 叉号标出** | 不要留个线头让人猜是"忘了连"还是"故意不连" |
| 5 | **排查假连通** | 两条线端点相距 1–5px 却不相连——视觉上是连的、电气上是断的，最危险 |

### ⚠️ 不要删「零长度导线」（2026-09-11 实测教训）

EasyEDA 的检查器有时会报 `零长度导线（首尾坐标相同，不连接任何东西，应删除）`。**在本工程里这是误报，绝不能删。**

实测：本图 (590,1035) 与 (1090,1125) 各有一条零长度导线，**它们就是那两个 GND 网络标号的"接点"**——删掉后标号立刻与导线脱开、对应网络掉线（GND 从具名网络退化成自动命名网）。而且**删了会重新长出来**：三次删除得到三个不同的图元 ID（`7acb82e3…`→`26965b85…`→`6cae2309…`），坐标始终不变。

结论：

- **零长度导线在本工程属于结构性连接，保留不动。**
- 检查器报这一条时**不要照做**；判据是看该点是否挂着网络标号——标号 + 零长度导线 = 标号的连接点。
- **渲染图逐像素比对一致不能证明连接性没变**（图形会完全一致，但电气上已经断开）——这类判断必须看网表。

**兜底动作**：画完跑一次 **ERC**，未接通的网络会直接报出来。

### ⚠️ 不要删「零长度导线」（2026-09-11 实测教训）

EasyEDA 的检查器有时会报 `零长度导线（首尾坐标相同，不连接任何东西，应删除）`。**在本工程里这是误报，绝不能删。**

实际验证：本图 U1 段 (590,1035) 与 U2 段 (1090,1125) 各有一条零长度导线，**它们就是那两个 GND 网络标号的"接点"**——删掉后标号立刻与导线脱开、对应网络掉线（GND 从具名网络退化成自动命名网）。而且**删了会重新长出来**：三次删除后是三条全新的图元 ID，但坐标始终不变。

结论：

- **零长度导线在本工程属于结构性连接，保留不动。**
- 检查器报这一条时**不要照做**；判据是看该点是否挂着网络标号——标号 + 零长度导线 = 标号的连接点。
- **渲染图逐像素比对证明不了连接性**（图形会完全一致，但电气上已经断开）——这类判断必须看网表。

### 踩坑记录（2026-09-11 实际发生）

同一张图上曾同时出现 5 处此类缺陷：3V3 旗标与电轨差 2px、VOUT 引线与电轨差 4px、VBAT 电轨右端多出 6px 悬空头、GND 地轨右端多出 14px 悬空头、电感引线接点漏打点。**全部是端点坐标没对齐造成的，肉眼几乎看不出。**

## 阶段 1：主控 + 电源 + 双 Type-C

### 必放器件

| 位号 | 器件 | 作用 |
| --- | --- | --- |
| U1 | ESP32-S3-WROOM-1-N16R8 | 主控 |
| U5 | TP4056 | 1S 锂电池充电 |
| U6 | DW01A + 外围电阻/电容 | 电池保护控制 |
| U7 | FS8205 | 电池保护 MOS |
| U2 | TPS63021（固定 3.3 V） | VBAT → 3.3V buck-boost |
| U9 | MAX17048 | I2C 电量计 |
| U10 | CH340N | Type-C COM 口 |
| U11/U12 | USBLC6-2SC6 | 两路 USB ESD 保护 |
| J3/J4 | USB Type-C 16P | COM 口 / 原生 USB 口 |
| D1/D2 | SS34 | 两路 5V 防倒灌 OR |
| BOOT1 / EN1 | 轻触开关 ×2 | BOOT（GPIO0，R5 上拉）/ RST（EN）；画布位号 BOOT1/EN1（2026-09-12 核对通过） |
| DS1 | 红/绿双色共阳 LED | 充电 / 充满指示 |

### 电源网络

- `VBUS1`：J3 Type-C VBUS → D1 阳极。
- `VBUS2`：J4 Type-C VBUS → D2 阳极。
- `5V_OR`：D1/D2 阴极汇合，供 TP4056 VCC。**CH340N VCC 不接 5V_OR（2026-09-12 修正：IO 电平跟随 VCC，5 V 会损坏 ESP32-S3）——接 `3V3`，V3 短接 VCC，详见 DESIGN.md「CH340N COM 口定稿」。**
- `VBAT`：电池正端、TP4056 BAT、TPS63020 VIN、MAX17048 Cell+、功放电源入口共用；电池负端经 DW01A + FS8205 保护后接系统 GND。
- `3V3`：TPS63020 输出，供 ESP32-S3、SD、墨水屏接口、I2C、音频小信号等。
- `GND`：公共地；后续 PCB 音频区做星型接地。

### 双 Type-C 要求

- J3/J4 的 CC1、CC2 各接一个 `5.1kΩ` 下拉到 GND。
- J3 的 D+/D- 经 U11 后接 CH340N 的 UD+/UD-（网络名 **`COM_DP`/`COM_DM`**，与 U1 原生 USB 的 `USB_DP`/`USB_DM` 区分，禁止同名并网）。
- J4 的 D-/D+ 经 U12 后接 U1 的 GPIO19/GPIO20。
- 符号细节（C165948）：DP1+DP2 并为 D+、DN1+DN2 并为 D-；VBUS 两脚并接；SBU1/SBU2 打 NC；外壳 EH1-4 接 GND；USBLC6 的 5 脚接 3V3（沁恒要求）。

### 双 Type-C 接线表（唯一依据，2026-09-12）

**J3 · COM 口**（TYPE-C-31-M-12，符号脚号以实际符号为准，按焊盘/名称接线）：

| J3 焊盘 | 名称 | 接到 | 该网络上的其他元件 |
| --- | --- | --- | --- |
| A4+B9 | VBUS① | VBUS1 | D1 阳极 |
| B4+A9 | VBUS② | VBUS1（与①并接） | 同上 |
| A6 | DP1 | D+ 汇合点 | DP2、U11.1 |
| B6 | DP2 | D+ 汇合点（与 DP1 并接） | 同上 |
| A7 | DN1 | D- 汇合点 | DN2、U11.3 |
| B7 | DN2 | D- 汇合点（与 DN1 并接） | 同上 |
| A5 | CC1 | R7 → GND | — |
| B5 | CC2 | R8 → GND | — |
| A8 | SBU1 | **NC 打叉** | — |
| B8 | SBU2 | **NC 打叉** | — |
| A1+B12、B1+A12 | GND ×2 | GND | 全板地 |
| EH1-4 | 外壳 ×4 | GND | 全板地 |

**U11 USBLC6-2SC6**：

| 脚 | 名称 | 接到 | 该网络上的其他元件 |
| --- | --- | --- | --- |
| 1 | I/O1 | D+ 汇合点 | J3.A6、J3.B6 |
| 3 | I/O2 | D- 汇合点 | J3.A7、J3.B7 |
| 6 | I/O1′ | **COM_DP** | U10.1（CH340N UD+） |
| 4 | I/O2′ | **COM_DM** | U10.2（UD-） |
| 5 | VCC | **3V3** | 画布 3V3 网络 |
| 2 | GND | GND | 全板地 |

**J4 · USB 口**（与 J3 完全同构，仅以下不同）：

| J4 焊盘 | 名称 | 接到 | 该网络上的其他元件 |
| --- | --- | --- | --- |
| A4+B9、B4+A9 | VBUS ×2 | **VBUS2**（两脚并接） | D2 阳极 |
| A6+B6 | DP1/DP2 | D+ 汇合点 | U12.1 |
| A7+B7 | DN1/DN2 | D- 汇合点 | U12.3 |
| A5 | CC1 | R9 → GND | — |
| B5 | CC2 | R10 → GND | — |
| A8、B8 | SBU1/SBU2 | **NC 打叉** | — |
| GND ×2、EH1-4 | 地/外壳 | GND | 全板地 |

**U12 USBLC6-2SC6**：

| 脚 | 名称 | 接到 | 该网络上的其他元件 |
| --- | --- | --- | --- |
| 1 | I/O1 | D+ 汇合点 | J4.A6、J4.B6 |
| 3 | I/O2 | D- 汇合点 | J4.A7、J4.B7 |
| 6 | I/O1′ | **USB_DP** | ⚠️ U1.14（GPIO20）标号需改名 USB_DP |
| 4 | I/O2′ | **USB_DM** | ⚠️ U1.13（GPIO19）标号需改名 USB_DM |
| 5 | VCC | 3V3 | 画布 3V3 网络 |
| 2 | GND | GND | 全板地 |

**D1/D2 与 CC 电阻**：

| 器件/脚 | 接到 | 同网络其他元件 |
| --- | --- | --- |
| D1 阳极 | VBUS1 | J3 VBUS 两脚 |
| D1 阴极 | **5V_OR** | D2 阴极、U3.4(VCC)、U3.8(CE)、C10（画布已有） |
| D2 阳极 | VBUS2 | J4 VBUS 两脚 |
| D2 阴极 | **5V_OR** | 同上 |
| R7 / R8 | J3.CC1 / J3.CC2 ↔ GND | — |
| R9 / R10 | J4.CC1 / J4.CC2 ↔ GND | — |

三条落地要点：① DP1+DP2（DN1+DN2 同理）先就近汇成一点、再单线去 USBLC6；② **画布改名：U1.13 标号 GPIO19→USB_DM、U1.14 标号 GPIO20→USB_DP**（03:48 核对已完成 ✓）；③ VBUS1/VBUS2 是两个独立网络，只能在 D1/D2 阴极汇成 5V_OR，禁止两口 5V 直接短接。**实际位号映射（04:06 重排定稿）：R6=J3.CC1、R7=J3.CC2、R8=J4.CC1、R9=J4.CC2（C2907269=5.1kΩ±1% 已核对），Type-C 段核对通过 ✓**。

### 电池保护与电量计接线表（唯一依据，2026-09-12；05:41 画布核对通过 ✓、05:51 C13 改接 BAT_N 复核 ✓；位号按实际：R10=100Ω/VCC 限流、R11=1kΩ/CS 限流、C13=VCC↔BAT_N、ALRT# 实接 GPIO18）

**U6 DW01A**（1 OD/2 CS/3 OC/4 TD/5 VCC/6 GND）：

| 脚 | 名称 | 接到 | 同网络其他元件 |
| --- | --- | --- | --- |
| 5 | VCC | R10(100Ω) → VBAT | — |
| 6 | GND | **BAT_N**（电池负极） | 电池负端、U7.S1 |
| 2 | CS(VM) | R11(1kΩ) → **GND**（系统侧，测 MOS 压降） | — |
| 1 | OD | U7.6（G1，放电控制） | — |
| 3 | OC | U7.4（G2，充电控制） | — |
| 4 | TD | **NC 打叉**（悬空） | — |

**U7 FS8205A（C2830320，共源）**：

| 脚 | 名称 | 接到 | 同网络其他元件 |
| --- | --- | --- | --- |
| 1 | S1 | **BAT_N**（电池负） | U6.6、C13 |
| 3 | S2 | **GND**（系统） | 全板地 |
| 2、5 | D（共端） | **悬空 NC**（内部互连） | — |
| 6 | G1 | U6.1 OD | — |
| 4 | G2 | U6.3 OC | — |

**U9 MAX17048**（1 CTG/2 CELL/3 VDD/4 GND/5 ALRT/6 QSTRT/7 SCL/8 SDA/EP）：

| 脚 | 名称 | 接到 | 同网络其他元件 |
| --- | --- | --- | --- |
| 1 | CTG | GND | — |
| 2 | CELL | VBAT | 电池正 |
| 3 | VDD | VBAT + C14→GND | — |
| 4、EP | GND | GND | — |
| 5 | ALRT# | **GPIO18**（U1.11，低电量中断；非 RTC 脚不可深睡唤醒） | U1.11 |
| 6 | QSTRT | GND | — |
| 7 | SCL | **GPIO2**（画布现有网名，勿新起） | U1.38 |
| 8 | SDA | **GPIO1** | U1.39 |

要点：① BAT_N 为新网络（电池负极），电池连接器 **J5**（BOM 缺口已补）1→VBAT、2→BAT_N；② 保护拓扑 = BAT_N → U7 背靠背 → GND，DW01A GND 在电池侧、CS 经 1kΩ 在系统侧；③ I2C 上拉 R11/R12 阶段 4 统一放，本段不画。

**J5 电池连接器接线表（选型 C52037422 = PH2.0-2PWT 讯普，卧贴侧接 2A 带定位焊脚）**：

| J5 脚 | 名称 | 接到 | 同网络其他元件 |
| --- | --- | --- | --- |
| 1 | VBAT+ | **VBAT** | U3.5、U2 VIN、U9.2/3、R10、功放电源 |
| 2 | BAT_N− | **BAT_N** | U6.6、U7.1、C13 |

要点：① **线序警告**——市售电池 PH2.0 插头线序无行业标准，到手必须万用表核对红线=1、黑线=2，反插会烧（DW01A 不防反）；② PH2.0 胶壳自带防呆凸起，机械防反 ✓；③ 电流余量：功放峰值 ~1.5A < PH 2A < U7 6A ✓；④ 丝印在 J5 旁标极性与 Li-ion 3.7V。⑤ 符号共 4 脚：1/2 信号 + **3/4 辅助定位焊脚（机械用，打 NC，勿接信号——若内部与触点导通，接错网会短路）**。

**墨水屏双兼容接线表（路线 C，2026-09-12）**：J1（1×9 排针，接带驱动板模块）与 J6+DNP 驱动区（24P FPC 座，接手上 YMS122250 裸屏）**共用 SPI 网络**，默认只焊 J1。

> **兼容域扩展（2026-09-12，用户驱动板评论区情报）**：该驱动板被实测可驱动 4.2 寸等其他尺寸 24P 裸屏——原因是 Solomon 家族（SSD1680/1677 及 UC8276 类）2.13–4.2 寸裸屏共用 **24P 0.5mm FPC 标准脚序**（GDR/RESE/VGH/VGL/VDH/VDL/VCOM/BS/TSCL/TSDA…），升压拓扑（L+3×二极管+NMOS+RESE）同款。即 **J1 + 此驱动板 ≈ 1.54–4.2 寸裸屏与带驱动板模块的准通用接口**。边界：7.5 寸以上 FPC 不同不适用；RESE 阻值固定 0.47Ω 非全屏最优；非官方承诺，换屏实测为准。

> **4.2 寸快刷结论（2026-09-12 查证）**：黑白 **GDEY042T81**（SSD1683，24P 0.5mm 同脚序，现役款，取代已 EOL 的 GDEQ042T81）官方规格：全刷 2s / 快刷 1.5s / 局部 0.3s / 4 灰度 2s，快刷波形内置 IC（调用即用）；固件用 GxEPD2 GxEPD2_420_GDEY042T91 类（useFastFullUpdate=true 启用 1.5s 快刷，低温场景设 false）。注意：连续快刷/局部 5 次后做一次全刷防残影；三色 4.2 寸默认按仅全刷对待（Good Display 新表称 BWR 支持 fast refresh+黑白局部，需按具体型号规格表确认）。硬件侧 J1+驱动板对快刷无额外要求。

> **4.2 寸淘宝选购指南（2026-09-12）**：GDEY042T81 贵约一倍=快刷 FPL 膜+内置波形溢价；便宜一半的黑白款多为老款 **GDEW042T2**（UC8176，24P 0.5mm 同脚序，全刷 3s/局刷 0.5s，无 1.5s 快刷，GD 官方已标长交期推荐换 T81）或 DKE 等同规格白牌。三色 4.2：GDEQ042Z21 红 19s / GDEH042Z96 红 16s / GDEW042C37 黄 28s，全部仅全刷。**没写型号的屏辨别三步**：① 问客服要驱动 IC 型号+规格书 PDF；② 收货看 FPC 丝印型号（GD 屏印 GDEW/GDEY…，DKE 屏印 DEPG…）；③ 确认 24P 0.5mm+Solomon/UC 家族 IC 才买——满足即插驱动板能驱动，固件换对应类。拆机价签屏最便宜但 FPC 脚序不保证，新手别赌。

> **淘宝三款 4.2 寸实查（2026-09-12，店：鑫盛液晶）**：① id=923737932787 Hink-E042A13-A0 黑白红三色 24pin 400x300，¥23，已售500+；② id=1009961968252 FPC-194 **黑白** 24pin 400x300，¥28，已售75，主图为价签画面（疑似价签同源/散新）；③ id=1008339886174 FPC-194 黑白红三色 24pin 400x300，¥25，已售400+，**主图显示 GOODISPLAY demo 画面（GD 同源，≈GDEQ042Z21/UC8276，全刷约19s）**。三款均未标驱动 IC；24P FPC → Solomon 标准脚序概率高，插驱动板可试，固件按实测 IC 换类。结论：静态显示选 ③（GD 同源最稳）；要局刷选 ②；要快刷仍只有 GDEY042T81。

| 网络 | J1 脚 | J6(FPC) 脚 | DNP 驱动件 |
| --- | --- | --- | --- |
| 3V3 | 1 | 15 VDDIO、16 VCI | 电容 1µF+0.1µF ×2 |
| GND | 2 | 17 VSS | 全板地 |
| DIN GPIO11 | 3 | 14 SDA | — |
| CLK GPIO12 | 4 | 13 SCL | — |
| CS GPIO10 | 5 | 12 CS# | — |
| DC GPIO9 | 6 | 11 D/C# | — |
| RST GPIO14 | 7 | 10 RES# | — |
| BUSY GPIO13 | 8 | 9 BUSY | — |
| CS2 GPIO15 | 9 | — | 双 CS 屏预留 |
| — | — | 2 GDR / 3 RESE | Q1 2N7002 + R15 0.47Ω（升压） |
| — | — | 5/18/19/20/21/22/23/24 | 电容组 C18–C31（泵/储能） |
| — | — | 1/4 NC、6/7 TSCL/TSDA | NC 悬空 |

要点：**默认只焊 J1（9 脚排针）**；接裸屏时补焊 J6+驱动区（DNP ×≈16）；两口 SPI 信号同名并网，**不可同时接屏**。

**J2 microSD 卡座接线表（方案 A 共享 SPI2，2026-09-12 出图；选型 C91145 = TF-01A 自弹 13 脚符号）**：

| 符号脚 | 名称 | 接到 | 备注 |
| --- | --- | --- | --- |
| 1 | DAT2(RSV) | **NC** | SPI 模式不用 |
| 2 | CDDAT3(CS) | **GPIO8** | SD_CS；R13 10kΩ 上拉到 3V3（已画布核对） |
| 3 | CMD(DI) | **GPIO11** | 与 J1 DIN(MOSI) 共网 |
| 4 | VDD | **3V3** | C15 0.1µF + C16 10µF 去耦到 GND |
| 5 | CLK(SCLK) | **GPIO12** | 与 J1 CLK 共网 |
| 6 | VSS | GND | — |
| 7 | DAT0(D0) | **GPIO21** | SD_MISO；R12 10kΩ 上拉到 3V3（已画布核对） |
| 8 | DAT1(RSV) | **NC** | — |
| 9 | CD | **NC** | 首版不做卡检测，固件轮询挂载 |
| 10–13 | GND（外壳） | GND | 四脚全部接地 |

要点：① CLK/MOSI 与 J1 共网（GPIO12/11），SD 独立 CS+MISO，与 BUSY（GPIO13 输入）不冲突——e-ink 等 BUSY 期间可自由读卡；② 上拉依据 = ESP-IDF SD SPI 驱动（CS 必须、MISO 建议）；③ 位号新用 R12/R13/C15/C16（画布已用到 R11/C14；R14 留 I2C SDA 上拉、R15 留 RESE、C17 缓冲、C18–C31 驱动区）；④ 2026-09-12 网表核对通过：J2 十个有网脚+三个 NC 全对，C15/C16 去耦对，R12 实际用于 MISO 上拉；**R13 已补画并复核（GPIO8/3V3）——J2 完全闭环（2026-09-12 复检：J1 九脚+J2 十脚+3NC 全对，floatingPins=0）**。遗留非阻塞：13 处导线交叉+15 条零长度导线待清图。

**J6+DNP 驱动区接线表（2026-09-12 出图；拓扑对照 WeAct 官方模块/GD 参考，全部 DNP）**：

| FPC 脚 | 名称 | 接到 | FPC 脚 | 名称 | 接到 |
| --- | --- | --- | --- | --- | --- |
| 1 | NC | 悬空 | 13 | SCL | GPIO12（共网） |
| 2 | GDR | 升压区 Q1 栅极 | 14 | SDA | GPIO11（共网） |
| 3 | RESE | R15 0.47Ω→GND | 15 | VDDIO | 3V3 + C20/C21 |
| 4 | NC | 悬空 | 16 | VCI | 3V3 + C18/C19 → L1 |
| 5 | VSH2 | C28 1µF→GND（三色） | 17 | VSS | GND |
| 6/7 | TSCL/TSDA | 悬空 | 18 | NC | 悬空 |
| 8 | BS | **GND**（选 4 线 SPI，必接） | 19 | VPP | C27 1µF→GND |
| 9 | BUSY | GPIO13 | 20 | VDH | C22 + D3 阴极 |
| 10 | RES# | GPIO14 | 21 | VGH | C23 + D2 阴极 |
| 11 | D/C# | GPIO9 | 22 | VDL | C24 |
| 12 | CS# | GPIO10 | 23 | VGL | C25 + D1 |
| — | — | — | 24 | VCOM | C26 |

泵拓扑（照 WeAct 参考，WeAct 用 AO3402+SS819WL+10µH）：VCI→L1(68µH)→SW；Q1 2N7002 D=SW/G=GDR/S=RESE 脚；RESE 脚→R15→GND；**D3: SW→VDH**；**C29: SW↔X 飞跨，D2: X→VGH**；**D1: X→VGL（负压级）**；VGH/VGL/VDL/VCOM/VSH2 各 1µF/25V 对地。要点：① BS 必须接 GND；② 二极管用肖特基；③ 焊接顺序：先 FPC 座+去耦，试屏再补泵区；④ 原理图已出（v1.1），待画布绘制+网表核对。

**J6 驱动区元件级接线表（v1.3 画图操作单，逐脚一列）**

J6（26 脚符号）：1=NC｜2=GDR｜3=RESE｜4=NC｜5=VSH2｜6=TSCL(空)｜7=TSDA(空)｜**8=BS→GND（必接）**｜9=BUSY→GPIO13｜10=RES#→GPIO14｜11=D/C#→GPIO9｜12=CS#→GPIO10｜13=SCL→GPIO12｜14=SDA→GPIO11｜15=VDDIO→3V3｜16=VCI→3V3｜17=VSS→GND｜18=NC｜19=VPP→VPP｜20=VDH→VDH｜21=VGH→VGH｜22=VDL→VDL｜23=VGL→VGL｜24=VCOM→VCOM｜25/26=定位脚→NC（勿接信号）

Q1 2N7002（SOT-23：1=G、2=S、3=D）：G→GDR｜S→RESE｜D→SW
L1 68µH：上端→3V3（勿新建 VCI 网）｜下端→SW
D3 正泵：阳极→SW｜阴极→VDH
D2 二级：阳极→X｜阴极→VGH
D1 负泵：阳极→X｜阴极→VGL
C29 飞跨：一端→SW｜另一端→X
R15 0.47Ω：一端→RESE｜另一端→GND

⚠️ SW 说明：SW=Switch Node（升压开关节点）的文字标注，不是开关元件、不是网络标签——L1 下端/Q1 漏极/D3 阳极/C29 上端四点就近导线直连即可，无需 SW 网络标签；若布局分开了必须用标签，命名 SW_NODE（避免与按键位号 SW3/SW4 联想混淆）。
C18/C19：VCI 侧去耦→3V3 与 GND
C20/C21：VDDIO 侧去耦→3V3 与 GND
C22：VDH→GND｜C23：VGH→GND｜C24：VDL→GND｜C25：VGL→GND｜C26：VCOM→GND｜C27：VPP→GND｜C28：VSH2→GND

**网络标签命名规则（2026-09-12 定，双 Type-C 场景）**：① **同一个口的重复脚用同名**（J3 两 VBUS 脚均挂 VBUS1、两 D+ 脚均挂 D+_COM）——同名自动并联，无需跨脚拉线，从此不再为交叉发愁；② **两个口之间必须不同名**（VBUS1/VBUS2、D+_COM/D+_USB）——这是 D1/D2 防倒灌隔离的前提，同名短接会让二极管失去意义、双充电器互灌；③ GND 全板共网、随便挂；④ CC1/CC2 不挂标签、各拉短线到自己的 5.1k 下拉（两条独立支路，勿并联）。

标签形式：3V3/GND 用电源符号（延续全图惯例）；VXX 七个与 GDR/RESE/SW/X 用**普通网络标签**——电源符号形式会触发 ERC「电源未驱动」误报（VDL 等网络无驱动源），且箭头形状对负压网络有误导。⑤ 符号共 4 脚：1/2 信号 + **3/4 辅助定位焊脚（机械用，打 NC，勿接信号——若内部与触点导通，接错网会短路）**。

**核对方法（2026-09-12 沉淀，每块必用；含流程强制项）**：**第 0 步必须是 easyeda sch read 重读活体网表**（禁止复用缓存 JSON——22:35 曾因旧快照漏报 D2.1 悬空），并先核对 check.summary 的 floatingPins/danglingWires 与结论互证；然后再做 ：除 easyeda sch read 的 check 汇总外，必须做 **①逐网络成员清点**（关键网络逐个列成员，确认无缺漏）与 **②孤立命名网络扫描**（成员数==1 的命名网络即疑似悬空标签）——**EasyEDA check 的 floatingPins 抓不到"孤立网络标签"**（2026-09-12 RESE 悬空实例：check 报 floatingPins=0 属假阴性）；孤立标签典型成因：网络标签只放在一端、另一端只有导线未挂标签。J6 驱动区实例：RESE 曾仅 J6.3 一个成员，补标签后为 J6.3+Q1.2+R14.2 三点。

- CH340N 的 TXD/RXD 接 U1 的 GPIO44/GPIO43，注意 TX/RX 交叉。
- 两路 VBUS 都必须经过 SS34 或等效理想二极管后汇成 `5V_OR`，禁止直接短接。

### 充电指示

- DS1 共阳极接 `3V3`，不接 `5V_OR`，避免 TP4056 状态脚被 10kΩ 拉到 3.3V 时 LED 两端仍有约 1.7V 压差导致微亮。
- 红光阴极经 `1kΩ` 接 TP4056 `CHRG`，充电中亮红。
- 绿光阴极经 `1kΩ` 接 TP4056 `STDBY`，充满亮绿。
- `CHRG` 经 `10kΩ` 上拉到 `3V3`，同时接 U1 GPIO17；低电平=充电中，可作深睡唤醒源。

### EN 与 3V3 接法（2026-09-11 依官方手册定稿，详见 DESIGN.md）

- **EN**：10 kΩ 上拉到 `3V3` + 1 µF 对地 + 复位按键到 GND（官方推荐 R=10 kΩ、C=1 µF）。GPIO0 同理：10 kΩ 上拉 + 按键到 GND。GPIO45/46 不要另加强上/下拉。
- **模组 3V3（第 2 脚）**：22 µF bulk（0805/1206，10 V，X5R/X7R）+ 0.1 µF（0402/0603，X7R），紧贴引脚，电容 GND 就近过孔。
- **U8 TPS63020**：L1 = 1.5 µH / Isat ≥ 5 A / 屏蔽式 4×4 或 5×5 mm；输入 2×10 µF（0805，16 V）；输出 3×22 µF（0805/1206，10 V）；VINA 旁路 0.1 µF（**不得 > 0.22 µF**）；3.3 V 反馈 R1 = 1 MΩ / R2 = 180 kΩ（或改用固定 3.3 V 的 TPS63021）。
- 电容一律 X5R/X7R，禁 Y5V/Z5U；耐压按 3.3 V 轨 ≥6.3 V（建议 10 V）、VBAT 轨 ≥10 V（建议 16 V）选，注意 DC bias 容量衰减。

### 主控关键网络

| U1 引脚 | 网络名 | 用途 |
| --- | --- | --- |
| EN | `EN` | 复位，接 RC + RST 按键 |
| GPIO0 | `BOOT` | 下载模式，按键到 GND，上拉到 3V3 |
| GPIO1 | `I2C_SDA` | MAX17048 / SHT30 |
| GPIO2 | `I2C_SCL` | MAX17048 / SHT30 |
| GPIO4 | `ENC_A` | 编码器 A |
| GPIO5 | `ENC_B` | 编码器 B |
| GPIO6 | `ENC_SW` | 编码器按压 |
| GPIO7 | `BTN1` | 用户按键 1 |
| GPIO8 | `SD_CS` | SD 卡片选 |
| GPIO9 | `EINK_DC` | 墨水屏 D/C |
| GPIO10 | `EINK_CS` | 墨水屏 CS |
| GPIO11 | `SPI_MOSI` | 墨水屏 DIN / SD MOSI |
| GPIO12 | `SPI_SCLK` | 墨水屏 CLK / SD CLK |
| GPIO13 | `EINK_BUSY` | 墨水屏 BUSY |
| GPIO14 | `EINK_RST` | 墨水屏复位 |
| GPIO15 | `EINK_CS2` | 双 CS 屏预留 |
| GPIO16 | `BTN2` | 用户按键 2 |
| GPIO17 | `CHRG_DET` | 充电检测 / 深睡唤醒 |
| GPIO19 | `USB_DM` | 原生 USB D- |
| GPIO20 | `USB_DP` | 原生 USB D+ |
| GPIO21 | `SD_MISO` | SD 卡数据回读 |
| GPIO38 | `I2S_DIN` | 播报数据 |
| GPIO39 | `AUDIO_SEL` | BT / S3 音源切换 |
| GPIO40 | `BT_STATUS` | 蓝牙连接状态 |
| GPIO41 | `AMP_EN` | 功放使能 |
| GPIO42 | `BT_UART` | 蓝牙模块 UART |
| GPIO43 | `TXD0` | COM 串口发送（画布网络名，2026-09-12 核对定稿；即 U0TXD） |
| GPIO44 | `RXD0` | COM 串口接收（画布网络名；即 U0RXD） |
| GPIO47 | `I2S_BCLK` | 播报位时钟 |
| GPIO48 | `I2S_LRCK` | 播报帧时钟 |

### 检查点

1. U1 每个使用的 GPIO 都有明确网络名，不使用悬空飞线。
2. `3V3` 至少在模组电源脚、TPS63020 输出、I2C、SD、墨水屏接口附近放置去耦电容。
3. GPIO0、GPIO45、GPIO46 等 strapping 脚不要接强上/下拉外设。
4. 模组天线区域只做封装净空，不放置金属器件、大面积铜箔或屏蔽罩。
5. 保存后让 Codex 解析工程，确认 documents/schematics 中出现元件与网络。
6. **U1 封装必须带底部中央热焊盘（EPAD，官方符号编为第 41 脚）**，且该焊盘归到 `GND` 网络。已实测立创器件 **C2913202** 的封装 `WIRELM-SMD_ESP32-S3-WROOM-1` 带此焊盘（3×3 九个 0.9 mm 焊盘，整体 3.7 × 3.7 mm），与官方 land pattern 一致，可直接用。**但该封装不含热过孔**——PCB 阶段必须在热焊盘下自己补 ≥9 个（推荐 16 个）0.5 mm 地过孔，否则热焊盘等于悬空铜皮。

## 阶段 2：墨水屏 + SD 卡

- J1 墨水屏接口：`3V3`、`GND`、`SPI_MOSI`、`SPI_SCLK`、`EINK_CS`、`EINK_DC`、`EINK_RST`、`EINK_BUSY`、`EINK_CS2`。
- J2 microSD：`3V3`、`GND`、`SPI_SCLK`、`SPI_MOSI`、`SD_MISO`、`SD_CS`。
- SD 的 `SD_CS`、`SD_MISO` 各接 `10kΩ` 到 `3V3`。
- 墨水屏接口最终脚序必须等实物确认后再定封装引脚顺序；当前先按网络名连接。

## 阶段 3：音频

### 阶段 3 音频区接线表（2026-09-13 定稿出图，逐脚唯一依据；引脚定义均已对照官方手册核实）

**拓扑决策（2026-09-13）**：BT 差分输出 → 每路 1µF 耦合 → 10k 电阻把 L/R 混成**差分单声道**（L+/R+ 混入 P 侧、L−/R− 混入 N 侧）→ 模拟开关 NO 触点；PCM5102A 输出 → 1µF 耦合 → 10k 混成单端单声道 → 开关 NC 触点；开关 COM 直入 NS4150C 的**差分输入** —— **差分转单端由功放的差分输入天然完成，全程不加运放**。开关 V+ 与功放 VDD 均取 VBAT（开关模拟摆幅范围 = 0~V+，取 VBAT 余量最大；耦合电容把模块侧直流与功放偏置彻底隔开，直流上互不干扰）。

**MOD1 EWM104-BT60SP3（24 脚，官方手册 3.1）**：

| 脚 | 名称 | 接法 |
| --- | --- | --- |
| 1 | AUDL_R（L+） | → C32(1µF) → `MIXLP` |
| 2 | AUDL_N（L−） | → C33(1µF) → `MIXLN`（2026-09-13 画布核对：用户按脚序 1/2/4/5 顺排耦合电容位号，与原设计表 C33/C34 互换，电气一致，**以画布为准**） |
| 3 | GND | → GND |
| 4 | AUDR_N（R−） | → C34(1µF) → `MIXRN`（同上，顺排） |
| 5 | AUDR_P（R+） | → C35(1µF) → `MIXRP`（同上，顺排） |
| 6 / 11 / 16 | K1/K2/K3 | **NC**（预留按键脚，低有效内部上拉） |
| 7 / 8 / 9 | MICBAIS/MIC_P/MIC_N | **NC**（不做麦克风） |
| 10 / 12 / 23 | GND | → GND（与 3 脚共四个地脚，全接） |
| 13 | MUTE | **NC**（预留；功放静音改由 GPIO41 控制） |
| 14 / 15 | NC | **NC** |
| 17 | VBAT | ← **3V3**（非电池直供）+ C29(10µF，C33546007)/C31(0.1µF 0402，C1525) 就近去耦（画布实际位号；原设计 C30 空缺未用） |
| 18 | VUSB | **NC**（模块内置充电输入，本项目不用；**严禁接 5V_OR**） |
| 19 | LED_A | → **GPIO40**（音频连接状态，高有效） |
| 20 | LED_L | **NC**（BLE 状态，预留） |
| 21 | RXD | ← **GPIO42**（S3 TX → 模块 RX，**AT 单线控制**） |
| 22 | TXD | → `BT_TXD` → **J8.1**（2026-09-13 变更：原 NC 改为引出调试排针）——J8 = 1×2P 2.54mm 直插（**C492401**，DNP 可选焊），J8.2→GND；台架插 USB-TTL **监听** AT 回执（只听不发，与 GPIO42 无冲突）；不焊排针 = 测试点焊盘，等效 NC。**画布动作：删掉 22 脚现有 NC 叉号 → 放 J8 → 1 脚挂 `BT_TXD`、2 脚接 GND** |
| 24 | ANT | **NC**（默认用板载 PCB 天线，**勿动内部跳线电阻**；天线区 keep-out 见 PCB 注） |

**U15 TS5A23157（DGS-10，TI 官方脚序：1 IN1 / 2 NO1 / 3 GND / 4 NO2 / 5 IN2 / 6 COM2 / 7 NC2 / 8 V+ / 9 NC1 / 10 COM1；IN=低→NC 通，IN=高→NO 通）**：

| 脚 | 名称 | 接法 |
| --- | --- | --- |
| 1 / 5 | IN1/IN2 | → `SEL`（= **GPIO39** + R25 10k 下拉→GND，**开机默认低=NC=PCM 播报路径**） |
| 2 | NO1 | ← `MIXP`（BT 差分 P 混音节点） |
| 9 | NC1 | ← `MIXS`（PCM 混音节点） |
| 10 | COM1 | → `AMP_INP` → U16.3 |
| 4 | NO2 | ← `MIXN`（BT 差分 N 混音节点） |
| 7 | NC2 | → C38(1µF) → GND（PCM 模式下 INN 交流参考地） |
| 6 | COM2 | → `AMP_INN` → U16.4 |
| 8 | V+ | ← **VBAT** + C39(0.1µF) 就近去耦 |
| 3 | GND | → GND |

**U16 NS4150C（SOP-8，官方脚序：1 CTRL / 2 Bypass / 3 INP / 4 INN / 5 VoN / 6 VDD / 7 GND / 8 VoP）**：

| 脚 | 名称 | 接法 |
| --- | --- | --- |
| 1 | CTRL | ← `AMP_EN`（= **GPIO41** + R26 10k 下拉→GND，**开机默认关断**消 pop；高=工作） |
| 2 | Bypass | → C40(1µF) → GND（内部共模旁路，手册要求） |
| 3 | INP | ← `AMP_INP` |
| 4 | INN | ← `AMP_INN` |
| 5 | VoN | → `SPK_N` → J7.2 |
| 8 | VoP | → `SPK_P` → J7.1（无滤波器架构，VoP/VoN **直连喇叭，不加 LC**） |
| 6 | VDD | ← **VBAT** + C41(10µF)/C42(0.1µF) 就近去耦 |
| 7 | GND | → GND |

**U14 PCM5102A（TSSOP-20，TI 官方脚序）**：

| 脚 | 名称 | 接法 |
| --- | --- | --- |
| 1 | CPVDD | ← 3V3 + C43(1µF→GND) |
| 2 / 4 | CAPP/CAPM | ↔ C44(1µF) 飞跨电容 |
| 3 | CPGND | → GND |
| 5 | VNEG | → C45(1µF→GND) |
| 6 | OUTL | → C36(1µF) → `MIXSL` |
| 7 | OUTR | → C37(1µF) → `MIXSR` |
| 8 | AVDD | ← 3V3 + C47(10µF)+C48(0.1µF) |
| 9 | AGND | → GND |
| 10 | DEMP | → GND（去加重关） |
| 11 | FLT | → GND（正常延迟滤波） |
| 12 | SCK | → GND（3 线模式，PLL 从 BCK 生成系统时钟） |
| 13 / 14 / 15 | BCK/DIN/LRCK | ← **GPIO47 / GPIO38 / GPIO48** |
| 16 | FMT | → GND（I2S 格式） |
| 17 | XSMT | ← 3V3（常开；无时钟时芯片自动静音，pop 由 AMP_EN 时序管理） |
| 18 | LDOO | → C46(1µF→GND) |
| 19 | DGND | → GND |
| 20 | DVDD | ← 3V3 + C49(1µF) |

**混音网络**：`MIXLP`—R19(10k)—→`MIXP`、`MIXRP`—R20(10k)—→`MIXP`；`MIXLN`—R21(10k)—→`MIXN`、`MIXRN`—R22(10k)—→`MIXN`；`MIXSL`—R23(10k)—→`MIXS`、`MIXSR`—R24(10k)—→`MIXS`。

**J7 喇叭连接器（新增，2026-09-13）**：**PH2.0-2PWT（C52037422，同 J5）**，1→`SPK_P`(U16.8)、2→`SPK_N`(U16.5)、3/4 定位脚 NC。单声道混音后喇叭相位无实际影响。

**新增元件（2026-09-13，位号接画布 R1-R18/C1-C28 之后）**：R19-R24（10k 混音 ×6）、R25/R26（10k 下拉 ×2）、C29/C31-C49（C29=10µF MOD1 去耦、C31=0.1µF 0402；**C30 空缺未用**——用户跳号；1µF 均为 0805 X7R ≥10V，10µF 0805 X5R ≥10V）。

**✅ MOD1 段核对通过（2026-09-13 01:00，活体网表 + SVG 双重验证）**：24 脚逐脚全对（17→3V3、四地脚接 GND、四路差分输出经 C32-C35 耦合出网、19→GPIO40、21→GPIO42）；**13 个 NC 脚（6/7/8/9/11/13/14/15/16/18/20/22/24）悬空且 SVG 叉号一一确认（全图 37 = 旧 24 + 新 13）**；孤立命名网络仅 4 个 MIX*（等 R19-R24/U15 续接，正常）+ 预留 GPIO。遗留非阻塞：老交叉 1 处（910,270）、零长导线 17 条（结构性忽略）、C27 极性提示（MLCC 忽略）。

**✅ J8 已画核对通过（2026-09-13 01:40，活体网表 + SVG）**：`BT_TXD = U4.22 + J8.1`（degree 2）、J8.2→GND（GND 85 成员）；U4 悬空脚 37→36（22 脚出列，余 12 个均为设计性 NC）；全图 X 形叉号 37→36（22 脚旧叉号已删）。

**✅ J8 料号已更换完成（2026-09-13 02:20 画布复核通过）**：C492401（XFCN 普通塑高）→ **C2982031（HCTL HC-2510-2A，高塑 11.6mm）**——理由：画布 J1 实际是 C2982038（HC-2510-9A 高塑座），初选时错拿了自己最早推荐的 C492408 当参照，导致风格不成套。活体网表复核：**J8 = C2982031 / 封装 CONN-TH_2P-P2.54_HCTL_HC-2510-2A，脚1→BT_TXD、脚2→GND，BT_TXD = U4.22+J8.1（degree 2），悬空脚 36、孤立网络仅 4 个 MIX*（待续）+ 预留 GPIO，全部符合预期**。与 J1 同厂同系列同塑高，外观成套（API 已核实封装，2 焊盘 1.8×1.8mm）。

**🎉 阶段 3 音频区全部核对通过（2026-09-13 03:49，活体网表 + 脚本逐项自动比对，210 器件 / 99 网络）**：
- **U14 PCM5102A（20 脚）、U15 TS5A23157（10 脚）、U16 NS4150C（8 脚）、J7、R19-R26、C36-C48 逐脚/逐件与设计拓扑完全一致**。位号被用户按摆放顺序重排（惯例），完整映射表见 BOM「音频链新增元件」：C37=飞跨、C38=DVDD、C39=VNEG、C40=LDOO、C41=CPVDD、C42=OUTR 耦合、C43=OUTL 耦合、C44=NC2 参考、C45=VBAT 高频、C46=Bypass、C47=VDD 主 10µF、C48=VDD 高频、C36=AVDD 高频。
- **全图孤立命名网络 = GPIO3/4/5/6/45/46（阶段 4 预留）——阶段 3 网络全部闭合**：4 个 MIX* 旗标已被 R19-R24 闭合、GPIO38/39/41/47/48 已接 U14/U15/U16。
- 悬空脚 38 = 设计性 NC 全集（此前 36 + J7.3/4），无一意外。
- **唯一缺件：AVDD 主去耦 10µF 未放**（3V3 轨只有 C36 0.1µF + C41 1µF；AVDD 仅 0.1µF 工作，按设计补 10µF 更稳）——**建议位号 C49（正好空缺）**，挂 3V3 靠 U14.8，料号 C33546007 同款。
- 遗留非阻塞：老交叉 1 处（910,270）、零长导线（结构性忽略）、C27 极性提示（MLCC 忽略）。
- **教训**：① 核对脚本按料号定位器件时，同料号器件会撞车（J5/J7 都是 C52037422，脚本把 J7 查成了 J5，虚报 2 项失败）——**按位号定位**；② 用户按摆放顺序重排位号是稳定惯例，核对以拓扑为准、位号映射回填文档。

**固件/调试注意事项**：
1. **音源切换消 pop 序列**：切 GPIO39 前先拉低 GPIO41（功放关断）→ 切换 → 再拉高使能。
2. **PCM5102A 输出 2.1Vrms@0dBFS 而功放增益内置不可调**——固件数字音量必须限制默认值，保证开关/功放输入节点摆幅在 0~VBAT 内，否则削顶失真。
3. GPIO39-42 为 S3 的 JTAG 引脚（MTCK/MTDO/MTDI/MTMS）——本项目调试走 USB-Serial-JTAG，无冲突（与"GPIO3 strap 不设"决策一致）。
4. PCB：① MOD1 天线区（24 脚附近）下方禁止铺铜/走线，模块尽量靠板边；② 功放输出 SPK_P/SPKN 走线短粗；③ 音频耦合电容与混音电阻靠近 U15 摆放；④ 模块下方禁止走线（手册第 6 章）。
5. 采购：喇叭 = **50mm / 4Ω / 3W 全频**（2026-09-13 定稿）；J7 插头与电池同款 PH2.0。

## 阶段 4：交互与传感器（2026-09-13 出图，逐脚接线表）

**✅✅ 阶段 4 全部核对通过 + 整张原理图完工（2026-09-13 04:55，活体网表终验，226 器件 / 99 网络）**：
- SW1（C2991196，ALPS EC11L1525G01）：A→GPIO4、B→GPIO5、C→GND、D→GPIO6、E→GND、6/7 定位脚→GND——7 脚全对。
- U5（SHT30-DIS-B10KS，C2687423）：SDA→GPIO1、SCL→GPIO2、ADDR→GND（0x44）、**R→GND**（手册硬要求）、VDD→3V3、VSS/EP→GND、ALERT/nRESET 双 NC——9 脚全对。
- C50（0.1µF，C1525）：3V3/GND ✅。
- **全图孤立命名网络 = GPIO3/45/46（strapping 预留，阶段 4 计划内不接线）——所有功能性网络闭合**。
- 悬空脚 40 = 设计性 NC 全集（36 既有 + J7.3/4 + U5.3/6），无一意外。
- **剩余收尾（转 PCB 前）**：① 清最后 1 处走线交叉（910,270）；② DNP 元件在 EDA 里批量勾 DNP 属性（J6/J8/Q1/L1/R14/C17/C19-C29、U5? 否——U5 实焊）；③ ERC 全跑一遍；④ 工程快照更新入库；⑤ 转 PCB（板框、模组天线净空、喇叭腔体先行）。

**SW1 EC11 旋转编码器（带按压）——2026-09-13 按画布实际符号（EC11L1525G01，7 脚：A/B/C/D/E/6/7）修正**：

| 符号脚 | 接法 | 备注 |
| --- | --- | --- |
| A | → **GPIO4** | 编码器 A 相；固件开内部上拉（~45k），免外部电阻 |
| B | → **GPIO5** | 编码器 B 相；同上；转向反了固件对调 A/B |
| C | → GND | 编码器公共端 |
| D | → **GPIO6** | 按压开关一脚（D/E 无极性，对调亦可）；内部上拉 + 固件消抖 20ms |
| E | → GND | 按压开关另一脚 |
| 6 / 7 | → **GND**（推荐）或 NC | 机械定位脚：与电路内部不通、纯固定用；金属件接 GND 加强固定与屏蔽，NC 亦无妨——焊盘照焊 |

**U13 SHT30-DIS 温湿度（2026-09-13 修正：实际符号 9 脚 = 1 SDA / 2 ADDR / 3 ALERT / 4 SCL / 5 VDD / 6 nRESET / 7 R / 8 VSS / 9 EP 底盘；逐脚依据 Sensirion 官方手册）**：

| 脚 | 名称 | 接法 | 备注 |
| --- | --- | --- | --- |
| 1 | SDA | → **GPIO1**（既有 I2C 总线） | 与 MAX17048 共线，地址不冲突 |
| 2 | ADDR | → **GND** | I2C 地址 = **0x44**（接 3V3 则 0x45）；**手册要求不可悬空** ✅ 已画 |
| 3 | ALERT | **悬空/NC** | 开漏报警输出，手册明示"不用时必须悬空" ✅ 已画 NC |
| 4 | SCL | → **GPIO2** | 上拉 R16/R17（4.7k）已有，勿重复放 |
| 5 | VDD | → **3V3** + **C50（0.1µF）** 就近去耦 | 手册要求 100nF 贴近 |
| 6 | nRESET | **悬空即可（建议打 NC）** | 低有效复位，**官方推荐不用时悬空**（内部上拉）；要接 3V3 须串 ≥2kΩ——本板选悬空 |
| 7 | R | → **GND** | ⚠️ 手册原文"无电气功能但**必须接 VSS**"——**不是 NC**！漏接不影响逻辑但违反手册 |
| 8 | VSS | → GND | |
| 9 | EP 底盘 | → **GND** | 内部已连 VSS；**机械上必须焊**（可靠性）✅ 已画 |

- SW3/SW4（画布位号）：分别接 GPIO7/GPIO16，另一端接 GND，使用内部上拉。✅ 已画核对通过（阶段 1）。
- I2C 总线上拉已由 R16/R17（4.7k）承担，不要每个器件重复放多组上拉。
- GPIO3/45/46 为 strapping 预留（慎用），不接线。
- 采购备注：SHT30 淘宝散新/尾货 3–4 元/颗可买（器件带 CRC 校验+出厂校准，假货读数直接离谱、风险低），但 DFN-8 手焊不现实——要么嘉立创贴片（用立创 C2687423），要么自备热风焊台。

## 原理图模块分区（2026-09-13 规划——画布整理用，挪动不影响电气）

> 画布用「图形→矩形」（无填充、虚线/细实线）画分区框 + 「文字」标模块名；框为纯视觉，不参与电气与 DRC。先挪元件再画框包住均可；**连接靠网络旗标，挪动零风险**（挪完我会重扫孤立网络确认）。

| # | 模块框 | 成员位号 | 建议画布位置 |
| --- | --- | --- | --- |
| ① | 电源区（充电/保护/3V3/电量计） | U3、R4、R2/R3、RED_1、GREEN_1、C10；U6、U7、R10/R11、C13、J5；U2、L3、C4-C9、C11；U9、C14、R18 | 左上（大框，可再画 4 个子框） |
| ② | USB 与串口区 | J3、J4、U10、U11、U12、D1、D2、R6-R9、C12 | 左下（Type-C 朝板边） |
| ③ | 主控最小系统 | U1、EN1、BOOT1、R1/R5、C1/C2/C3、SW3、SW4 | 中上（U1 天线朝板角） |
| ④ | 显示接口区（含 DNP） | J1、J6、Q1、L1、R14、D3-D5、C17-C28 | 右上（FPC 朝板边） |
| ⑤ | 存储区（SD） | J2、C15/C16、R12/R13 | 中下（与④共享 SPI，放相邻） |
| ⑥ | 音频链区（最大） | U4、C29/C31-C35、U14、C36-C43、C49、U15、R19-R25、C44/C45、U16、R26、C46-C48、J7、J8 | 右下（J7/J8 朝板边） |
| ⑦ | 交互与传感区 | SW1、U13、C50 | 中下（与⑤相邻） |

**框间主要信号流**（挪位时让有连线的模块相邻，飞线最短）：②→③ USB_DP/DM + TXD0/RXD0；③→④ SPI（GPIO9-15）；③→⑤ SPI 共线（GPIO8/11/12/21）；③→⑥ I2S（GPIO38/47/48）+ GPIO39-42；③→⑦ GPIO4-6/7/16 + I2C（GPIO1/2）；①→全区 电源轨（3V3/VBAT/GND/5V_OR 用旗标对接，无需拉线）。

## 阶段 5：检查与转 PCB

1. ERC：电源网络、输入悬空、重复位号、未连接引脚。
2. BOM：核对每个位号的封装、值、耐压、封装尺寸。
3. 网络表：对照 `docs/PIN_MAP.md` 逐项检查 GPIO。
4. 转 PCB 后先画板框、模组天线净空、喇叭腔体位置，再布局布线。


**器件必备外围件清单（2026-09-12 建立，核对时逐项打勾——防"引脚都对但缺件"）**：

| 器件 | 必备外围件 | 现状 |
| --- | --- | --- |
| U1 ESP32-S3-WROOM-1 | 3V3 去耦（10µF+0.1µF）、EN RC（R1+C3）、GPIO0 上拉（R5）、GPIO35-37 悬空 | ✓ |
| U2 TPS63021 | 输入电容（C4-C6/C11/C14）、输出电容（3V3 侧）、电感 L3、EN/FB 接法 | ✓ |
| U3 TP4056 | VCC 去耦（C10）、PROG（R4）、TEMP 接地、CE 接 VCC | ✓ |
| U6 DW01A | VCC 经 R10+C13、VM 经 R11 到地 | ✓ |
| U7 FS8205 | 共漏结构，D1/D2 悬空正确 | ✓ |
| U9 MAX17048 | VDD 去耦（C14）、CTG/QSTRT 接地、**I2C 上拉 R16/R17**、**ALRT# 上拉 R18** | ✓（2026-09-12 补） |
| U10 CH340N | VCC 去耦、V3 短接 VCC（3V3） | ✓ |
| U11/U12 USBLC6 | VBUS 参考脚（接 3V3 与被保护线路电平一致）、GND | ✓ |


**零长导线 = 不要清理（2026-09-12 用户实测结论）**：EasyEDA check 报的 "zero-length-wire"（全图约 16 条）**不是垃圾线段**，而是 **GND 符号/网络标签的附着锚点**——用户实测删除后对应 GND 断开。**今后一律忽略此项、不得列入待清清单**（此前多轮"清理零长导线"的建议作废）。另注：用坐标找最近元件来给用户描述位置的方法对锚点无效（锚点在符号内部、与元件原点偏差大），不再使用。

**polarityConventionOutliers 提示的定位**：该项无坐标（at=None），含 count 计数。2026-09-12 实例 = count 18 中含 **RED_1/GREEN_1 两个 LED 极性接反**（BOM 已标注"方向接反"）——LED 正确接法：3V3→LED 阳极→阴极→限流电阻→CHRG#/STDBY#（开漏拉低时点亮）；接反则永不亮。改法：选中 LED 旋转 180°。
