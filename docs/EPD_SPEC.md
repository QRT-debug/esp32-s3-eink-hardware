# 2.7" 墨水屏应用参数速查（板上屏）

> **依据**（一手）：Waveshare 官方规格书 **`2.7inch e-Paper (B)` Rev 1.0 / 2020-10-28**（29 页，本地副本 `Temp/epd27_bv2_spec.pdf`）+ Waveshare 官方 Selection Guide / 产品页。
> **本文只列应用参数**（选型 / 结构 / 固件用），**不含电路设计** —— 屏的泵电路与外围取值见 `docs/SCHEMATIC_CHECKLIST.md`。
> ⚠️ **板上两路屏接口共享同一组 SPI、同一时间只能接一个**：
> - **`J6` = 24P FPC 座，直插裸屏**（本表对应型号）
> - **`J1` = 9P 排针，插微雪驱动板模块**（可配 2.13" 三色 / 2.7" 黑白，见文末对照）

---

## 零、本项目的两块屏（速查）

| | **2.13"（手上参考屏）** | **2.7" (B)（板上主用）** |
|---|---|---|
| 型号 | **YMS122250-0213BAAMFGN** | Waveshare `2.7inch e-Paper (B)` |
| 分辨率 | 250 × 122 | 264 × 176 |
| 颜色 | 黑白红三色 | 黑白红三色 |
| **驱动 IC** | **SSD1680**（兼容命令集，固件实测） | **SSD1680Z8** |
| 外形 / 有效显示区 | 29.2 × 59.2 × 0.9 / 23.71 × 48.55 mm | 70.42 × 45.80 × 0.98 / 57.288 × 38.192 mm |
| 全刷 | 约 21 s | 15 ~ 17 s |
| 局刷 / 快刷 | ❌ 均不支持 | ❌ 均不支持 |
| 接口 | 24P FPC / 0.5 mm（裸屏 COG，SSD1680 内嵌玻璃） | 24P FPC / 0.5 mm |
| 板上接法 | `J1`（经微雪驱动板模块）／`J6`+泵区（DNP 预留） | `J6` |
| 工作 / 存储温度 | 0~+40 °C / −25~+40 °C | 0~+40 °C / −25~+40 °C |

**★ 两屏同属一个 IC 家族（Solomon **SSD1680**）⇒ 外围配套值完全通用**：

```
RESE = 2.2 Ω（1%）    升压电感 = 47 µH 级（Io≤500 mA；微雪/本板用 68 µH）
N-MOS = Si1308EDL（可替 AO3400）    肖特基 = MBR0530
```

这也解释了为什么**微雪同一块驱动板能同时"官配" 2.13" 与 2.7"** —— 它俩本来就是一族的。
> ⚠️ 与之相对，**UC 系列（Ultrachip）是另一族**：RESE **0.47 Ω** + 电感 **10 µH（Io≤1 A）**，两族的值**不能互换**。

---

## 一、识别

| 项 | 值 |
|---|---|
| 型号 | **2.7inch e-Paper (B)**（三色：黑 / 白 / 红） |
| 驱动 IC | **SSD1680Z8** |
| 规格书 | Rev 1.0 / 2020-10-28 |
| 接口 | 3-wire SPI / 4-wire SPI（本板用 **4-wire**，BS1 接 GND） |
| FPC | **24 pin，0.5mm 间距** |

## 二、显示与机械

| 参数 | 值 |
|---|---|
| 屏幕尺寸 | 2.7 英寸 |
| **分辨率** | **264 × 176**（机械图注：264 gate × 176 source；规格书表写作 "176(H)×264(V)"） |
| DPI | 118 |
| **有效显示区** | **57.288 × 38.192 mm** |
| 像素间距 | **0.217 × 0.217 mm** |
| 像素形状 | 矩形（Rectangle） |
| **外形尺寸** | **70.42 × 45.80 × 0.98 mm**（`70.42 (V) × 45.8 (H) × 1.0 (D)`） |
| **重量** | **5.0 ± 0.2 g** |
| 颜色 | 黑 / 白 / **红**（1-bit 三色） |
| **灰阶** | **2 级（无灰阶）** |
| 视角 | 超宽（纯反射式；规格书未给具体角度值） |
| 表面 | 硬涂层防眩光（Hard-coat antiglare） |

## 三、光学指标

| 参数 | Min | **Typ** | Max |
|---|---|---|---|
| 白色反射率 | 30 % | **34 %** | — |
| 对比度（室内） | 15:1 | **20** | — |
| 黑 L* | — | **13** | 15 |
| 白 L* | 62 | **65** | — |
| 红 L* | 27 | **28** | 32 |
| 黑 A* | — | **4** | 6 |
| 白 A* | — | **0** | 1 |
| 红 A* | 36 | **40** | 45 |
| 黑/白鬼影 ΔL | — | **1** | — |
| 红鬼影 ΔE | — | **3** | — |
| 24 h 变色（W/K/R） | — | **ΔE < 2** | — |
| **寿命** | — | **5 年**（23±3 °C / 55±10 %RH） | — |

## 四、刷新

| 参数 | 值 |
|---|---|
| **全屏刷新（典型，23 °C）** | **17 s**（规格书 p5）／ **15 s**（Waveshare 产品页口径） |
| **局部刷新** | ❌ **不支持** |
| 刷新模式 | Display Mode 1 / Mode 2（均基于 LUT，全屏） |
| 刷新期间约束 | **BUSY 为高时不得发命令**，否则画面损坏 |
| 双稳态 | ✅ 断电后画面保持 |

**黑白版对照（重要，决定换屏可能）**：

| | 2.7" (B) 三色（本板） | 2.7" 黑白 |
|---|---|---|
| 颜色 | 黑/白/红 | 黑/白 |
| **灰度** | **2 级** | **4 级** |
| **全刷** | **15~17 s** | **6 s** |
| **局部刷新** | ❌ | **✅** |
| 分辨率 / 显示区 / 外形 | 完全相同（264×176 / 57.288×38.192 / 70.42×45.80×0.98） | 同左 |

> 即：**外形与分辨率完全互换，但功能差一档** —— 想用局刷/灰度就换黑白版（走 J1 驱动板模块那条路）。

## 五、功耗

| 参数 | 值 |
|---|---|
| 典型功率 | **12.6 mW**（@3.0 V；水平 3 阶图案→垂直 3 阶图案切换测得） |
| 刷新功耗（官方产品页口径） | 26.4 mW typ |
| 典型工作电流 | **4.2 mA** |
| **峰值电流** | **50 mA typ / 60 mA max** |
| Sleep 模式（DC/DC off、无时钟、RAM 保持） | **20 µA** |
| **Deep sleep**（RAM 不保持） | **1 µA typ / 5 µA max**（功率 0.003 mW） |
| 待机（保持画面） | 断电即可，不耗电 |

## 六、供电与温度

| 参数 | Min | **Typ** | Max |
|---|---|---|---|
| 逻辑供电 **VCI** | 2.3 | **3.0** | 3.6 V |
| 核心 **VDD**（内部由 VCI 稳压） | 2.3 | **3.0** | 3.6 V |
| VCI 绝对最大额定 | −0.3 | — | +6.0 V |
| 逻辑高 VIH | 0.7 × VCI | — | VCI |
| 逻辑低 VIL | 0 | — | 0.3 × VCI |
| **工作温度 TOPR** | **0** | — | **+40 °C** |
| **存储温度 TSTG** | **−25** | — | **+40 °C** |
| **最优存储** | — | **23 ± 3 °C / 55 ± 10 %RH** | — |

**温度细则（易被忽略）**：
1. **0–35 °C 才保证单像素显示质量**；35–40 °C **仅保证条码可读**。
2. 存储 **−25~0 °C 或 40~60 °C 时只能存 10 天**。
3. **存放必须白屏朝上**（规格书两处强调）。
4. 运输环境湿度 45~70 %RH 时可存 30 天，越界 10 天。

> 屏内泵产生的电压（外部只需挂电容）：**VSH = +15 V / VSL = −15 V / VGH = +22 V / VGL = −20 V**。

## 七、引脚定义（24 pin，4-wire SPI）

| Pin | 名称 | I/O | 说明 |
|---|---|---|---|
| 1 | NC | — | 悬空（勿与其他 NC 相连） |
| 2 | GDR | O | N-MOS 栅驱动（升压开关控制） |
| 3 | RESE | I | 控制环检流输入 |
| 4 | NC | — | 悬空 |
| 5 | **VSH2** | C | 正源压（**红**）—— 仅三色屏用 |
| 6 | TSCL | O | 外部数字温度传感器 I²C 时钟 |
| 7 | TSDA | I/O | 外部数字温度传感器 I²C 数据 |
| 8 | **BS1** | I | 接口选择：**低 = 4 线 SPI（8 bit）**，高 = 3 线 SPI（9 bit） |
| 9 | **BUSY** | O | 忙标志（高 = 忙；输出波形或与温度传感器通信时拉高） |
| 10 | RES# | I | 复位，**低有效** |
| 11 | **D/C#** | I | 高 = 数据，低 = 命令 |
| 12 | CS# | I | 片选，低有效 |
| 13 | SCL | I | SPI 时钟 |
| 14 | SDA | I/O | SPI 数据 |
| 15 | VDDIO | P | 接口电平供电，**应与 VCI 同电位** |
| 16 | VCI | P | 芯片供电 |
| 17 | VSS | P | 地 |
| 18 | VDD | C | 核心逻辑电源（内部由 VCI 稳压，仅挂电容） |
| 19 | **VPP** | P | **FOR TEST / Keep Open**（保持悬空） |
| 20 | VSH1 | C | 正源压储能 |
| 21 | VGH | C | 正栅压（兼 VSH1）储能 |
| 22 | VSL | C | 负源压储能 |
| 23 | VGL | C | 负栅压（兼 VCOM / VSL）储能 |
| 24 | VCOM | C | VCOM 储能 |

## 八、SPI 接口时序（4-wire 写模式）

| 参数 | 值 |
|---|---|
| **SCL 频率（写）** | **≤ 20 MHz** |
| SCL 频率（读） | ≤ 2.5 MHz |
| tCSSU（CS# 在首个 SCL 上升沿前为低） | ≥ 60 ns |
| tCSHLD（CS# 在最后一个下降沿后保持低） | ≥ 65 ns |
| tCSHIGH（两次传输间 CS# 保持高） | ≥ 100 ns |
| tSCLHIGH / tSCLLOW（SCL 高/低电平） | ≥ 25 ns / 25 ns |
| tSISU（SI 建立） | ≥ 10 ns |
| tSIHLD（SI 保持） | ≥ 40 ns |

## 九、可靠性与质检

**可靠性试验**（全部 240 h）：低温存储 −25 °C ／ 高温存储 60 °C·40 %RH ／ 高温工作 40 °C·35 %RH ／ 低温工作 0 °C ／ 高温高湿工作 40 °C·80 %RH ／ 高温高湿存储 50 °C·80 %RH ／ **温循 [−25 °C 30 min]→[+60 °C 30 min] × 50 次** ／ **ESD 空气 ±4 kV、接触 ±2 kV**（裸屏，含 IC 与 FPC 区）。

**坏点标准（Zone A）**：
- 黑/白点：D ≤ 0.25 mm 允许；0.25 < D ≤ 0.4 mm 最多 4 个；**D > 0.4 mm 不允许**
- 黑白线：L ≤ 0.4 mm 且 W ≤ 0.1 mm 可忽略；稍大最多 4 条；**L > 1.0 mm 或 W > 0.4 mm 不允许**
- 切换过程中的鬼影、闪点允许（FPL 大于视区亦允许）
- 短路 / 断路 / 显示异常：**不允许**

**包装**：35 pcs/层 × 12 层 = **420 pcs/箱**，16 箱/托盘 = **6720 pcs**；**运输须白图或黑图**。

**环境认证**：RoHS。

## 十、使用与存放注意（工程必读）

1. **存放必须白屏朝上**，否则长期存放损伤屏体。
2. **玻璃易碎**，勿跌落/撞击；破碎后**勿接触电泳材料**，沾到用水与肥皂清洗。
3. **对静电敏感**（裸屏无防护）⇒ 操作需防静电措施。
4. **不可拆解模组**（会永久损坏并失去保修）。
5. **避免酸碱等腐蚀性气体**。
6. ⚠️ **规格书免责条款**：所列参数**仅保证模组本身**；后装配（贴合、压合、结构件挤压）可能影响性能或造成损坏，装配后不再保证。
7. **刷新长达 15~17 s** 且**三色无局刷** ⇒ 交互设计必须容忍；每次画面变化都是全屏刷新。

## 十一、由参数直接推出的三条固件约束

1. **必须按"全屏重画"设计**（无局刷）：整帧 264 × 176 × 1 bit = **5.8 KB**，三色双缓冲（黑白 + 红）= **≈11.6 KB**。
2. **SPI ≤ 20 MHz**：11.6 KB 在 20 MHz 下传输仅约 5 ms ⇒ **瓶颈完全在 15~17 s 的波形刷新，不在 SPI**。
3. **BUSY 必须等**（且加超时保护）：刷新期间发命令会损坏画面；建议 BUSY 中断 + 超时兜底。

---

## 十二、2.7" 黑白版（非 B）—— 参数对照与互换性

> 依据：Waveshare `2.7inch e-Paper` 官方规格书（另一份，本地 `Temp/ws27_bw_spec.pdf`，37 页）。
> **结论先行：板上 `J6` 可以直接插 2.7" 黑白版裸屏，PCB 不用动。**

### 12.1 参数对照

| 项 | 三色 (B)（本板现用） | 黑白（非 B） |
|---|---|---|
| 分辨率 / 显示区 / 外形 | 264×176 / 57.288×38.192 / 70.42×45.80×0.98 | **完全相同** |
| **灰度** | 2 级 | **4 级** |
| **全刷 Full** | 15~17 s | **3 s** |
| **快刷 Fast** | — | **1.5 s**（刷新时闪一次） |
| **局刷 Partial** | ❌ 不支持 | **0.42 s**（刷新时不闪） |
| 连续局刷限制 | — | **建议每 5 次后补一次全刷**，抑制残影累积 |
| **工作温度** | 0 ~ +40 °C | **0 ~ +50 °C** |
| **存储温度** | −25 ~ +40 °C | **−25 ~ +70 °C** |
| 最优存储 | 23±3 °C | 23±2 °C |
| VCI | 2.3 / 3.0 / 3.6 V | 2.2 / 3.0 / 3.7 V |
| VCI 绝对最大 | −0.3 ~ +6.0 V | −0.5 ~ +6.0 V |
| 内核 VDD（内部稳压，外部只挂电容） | 3.0 V | **1.8 V**（1.7~1.9） |
| 逻辑阈值 | VIH ≥ 0.7 VCI / VIL ≤ 0.3 VCI | VIH ≥ 0.8 VCI / VIL ≤ 0.2 VCI |
| 睡眠 / 深睡电流 | 20 µA / 1~5 µA | **相同** |
| 深睡功率 | 0.003 mW | 相同 |
| 典型功率 / 工作电流 | 12.6 mW / 4.2 mA | **规格书标 TBD** |
| 峰值电流 | 50 / 60 mA | **规格书标 TBD** |
| 泵电压表（Driver IC Internal Regulators） | 有（VSH/VSL/VGH/VGL） | **规格书未列** |

### 12.2 为什么 `J6` 能直驱（三条证据）

1. **24 脚定义逐脚一致** —— 两份规格书 §5 引脚表实测对比，**完全同序**（1=NC、2=GDR、3=RESE、4=NC、5=VSH2、6=TSCL、7=TSDA、8=BS1、9=BUSY、10=RES#、11=D/C#、12=CS#、13=SCL、14=SDA、15=VDDIO、16=VCI、17=VSS、18=VDD、19=VPP、20=VSH1、21=VGH、22=VSL、23=VGL、24=VCOM），连 `VSH2 (Red)` 脚都保留。
2. **同一驱动 IC 家族** —— 黑白版规格书的操作序列引用 **Command 0x11 / 0x44 / 0x45 / 0x3C / 0x18 / 0x22 / 0x20**，与三色 (B) 版命令表逐条吻合（UC81xx 系列命令集完全不同，可排除）。屏的泵由 IC 内部控制环 + 外部 GDR/RESE 功率级构成，同族 IC ⇒ 同一套泵方案。
3. **微雪官方同一块驱动板同时支持** —— 本板 `J1` 记录：微雪驱动板 v1.4「官配 2.13" 三色 / **2.7" 黑白**」，说明 L ≈68 µH + MBR0530 + RESE 这套外部泵在 2.7" 黑白上同样成立。

> 另有两条佐证：黑白版同样声明 "On-chip booster and regulator control for generating VCOM, Gate and Source driving voltage"；且黑白版比三色**少一个电压轨**（不需要 VSH2 红膜压），对泵的负担只会更轻。

### 12.3 换屏时要做什么

| 层面 | 要做什么 |
|---|---|
| **PCB** | ✅ **不用动**（封装、24 脚定义、泵拓扑全部兼容） |
| **元件** | `C28`（挂在 VSH2 脚、给三色红膜储能）黑白版不需要 ⇒ **可不贴**；其余照旧 |
| **固件** | 换 LUT/波形来源与刷新流程（黑白版可 `0x22`/`0x20` 从 OTP 载入或 MCU 下发）；启用局刷与 4 级灰度；**局刷每 5 次补一次全刷** |
| **待复核项** | 黑白版参考电路的 **RESE 值**（该页为图片、无文本层，程序读不到）。同族 IC 应为 **2.2 Ω**，与板上 `R14` 一致 |

### 12.4 泵驱动能力：本板泵驱 2.7" 黑白够不够？（2026-09-20）

**结论：够用，而且比三色更轻松 —— 换黑白是给泵"减负"，不是加负。**

**① 黑白版同样靠外部泵（规格书自证，非推断）**

2.7" 黑白规格书自己的引脚表（§5 Input/Output Pin Assignment，p7–8）：

| Pin | Name | I/O | Description |
|---|---|---|---|
| 2 | **GDR** | O | N-Channel MOSFET Gate Drive Control |
| 3 | **RESE** | I | **Current Sense Input for the Control Loop** |
| 4 | NC | — | Keep Open |
| 5 | VSH2 | C | Positive Source driving voltage(Red) |
| 20 | VSH1 | C | Positive Source driving voltage |
| 21 | VGH | C | Positive Gate driving voltage & VSH1 |
| 22 / 23 | VSL / VGL | C | Negative Source / Gate driving voltage |
| 24 | VCOM | C | VCOM driving voltage |

⇒ 与三色版**逐脚相同**，且功能描述写明"On-chip booster and regulator control"——**泵仍是外部那套**（L + NMOS + 3×MBR0530 + RESE + 储能），与 §12.2 的三条证据一致。

**② 本板泵 vs 原厂推荐：逐项达标或更优**（原厂表见 §14.1，本板实装见 §14.5）

| 项 | 原厂（SSD1680 Table 13-1） | 本板实装 | 判定 |
|---|---|---|---|
| 升压电感 | 47 µH / Io ≤ 500 mA | **68 µH**（值更大 ⇒ 纹波更小） | ✅ |
| **RESE** | **2.2 Ω / 1 %** | **2.2 Ω ±1 %** | ✅ 完全一致 |
| N-MOS | Si1304BDL（Rds(on) ≤ 2.1 Ω） | **AO3400A（≈30 mΩ）** | ✅ 远优 |
| 二极管 | MBR0530 ×3（Io ≥500 mA） | MBR0530 ×3 | ✅ |
| 泵储能电容 | 1 µF（≥25 V） | 1 µF / **50 V** | ✅ 更优 |
| 飞跨电容 | 4.7 µF / 50 V | 4.7 µF / 50 V（C26） | ✅ |

**③ 为什么黑白比三色更"轻"**

| 维度 | 三色（现用） | 黑白 | 对泵的影响 |
|---|---|---|---|
| 高压轨数 | VSH1 + **VSH2（红膜正源压）** + VSL + VGH + VGL + VCOM | **少 VSH2 一路** | 黑白更轻 |
| 有效区 / 像素 | 2188 mm² / 46,464 px | 完全相同 | 栅、源容性负载相同 |
| 全刷时间 | 15~17 s | **3 s** | 泵工作占空**大幅下降** |
| 重复刷新 | 无局刷 | 局刷 0.42 s | 单次搬运的电荷量更小 |

**④ 微雪官方已为这条组合背书**

`J1` 的官配微雪驱动板官方标称支持 **2.13" 三色 / 2.7" 黑白**，而本板泵与该板同源（§12.2 证据③）⇒ 68 µH + 2.2 Ω 这套驱 2.7" 黑白是**官方已验证过的用法**。

**⑤ 唯一的余量瓶颈（与"换黑白"无关）**

- 本板 `L1`（换料前 68 µH）的**额定电流 380 mA**（LCSC 口径；规格书**不单列 Isat**，其 IDC 定义为"感量降 25 % / 温升 40 ℃ 取较小者"，见 §12.7.6），约为官方参考件温升额定（**480 mA**）的 79 %；换料后 470 mA（98 %）。⚠️ 旧版此处写"Isat = 380 mA / 原厂 60 %"系**口径混淆 + 算错**，已在 §12.7.6「二次核查」更正。
- 但这是**现有硬件的既有问题**：三色同样吃它，**换黑白不会让它变差**（占空更低）。
- 想加余量 ⇒ 换感量 47 µH、且**参数表里明确给出 Isat** 的功率电感（同为 3.5×3.0 封装可直接换料不改板；只有更大封装才需改布局，属 V2）。

**⑥ 首件验证（与 §15.6 同一清单，黑白版口径）**

1. 插 `J6` 黑白屏 → 发一次全刷
2. 量 `VGH` / `VGL`（黑白版数值以其 OTP 波形为准，与三色同量级 ≈ ±20 V）
3. 记刷新时间：应 ≈ **3 s**；明显 > 5 s 即泵能力不足
4. 摸 `L1` 温度（发烫＝饱和迹象）
5. `C28`（挂在 VSH2 上的 1 µF）黑白用不到 ⇒ **可不贴**

> ⚠️ 如实标注：黑白版规格书的 **参考电路（p30）与 Panel DC Characteristics 表都是图片**，文本层读不到，所以其 RESE 的**具体数值仍是"同族 IC ⇒ 2.2 Ω"的推断**，不是实测。另外**本板泵从未实测**（板子未打样，此前一直走 `J1` + 微雪模块）。以上是"规格书级 + 同源设计"的判断。旁证是有利的：§13 里那次把驱动板 RESE 改到 3 Ω（偏大 36%）仍能正常驱动 2.13"，说明这一族对 RESE 的容忍度比标称宽。

### 12.5 2.7" 黑白屏的采购候选与筛选红线（2026-09-21 查证）

**候选（参数均取自厂家官方规格书）**

| 型号 | 驱动 IC | 关键参数 | 供货状态 | 判定 |
|---|---|---|---|---|
| **`GDEY027T91`** ⭐ | **SSD1680** | 264×176 / 外形 **70.42×45.8×1.0** / 有效区 57.288×38.192 / 点距 0.217 / **24 Pin FPC 0.5 mm** / 全刷 **3 s** / 快刷 **1.5 s** / 局刷 **0.42 s**（**GDEY027T91 规格书 p10 口径**；早前写 0.3 s 系佳显官网数字，2026-09-21 已更正）/ **4 灰阶** / 刷新功耗 **9 mW** / 待机 0.003 mW / 0~50 ℃ | **现货**（佳显标 48 h 发货；欧洲 LaskaKit 现货 €10.60 ≈ ¥82） | **首选** |
| `GDEW027W3` | **EK79652**（天德钰 JADARD） | 同尺寸同 24P / 全刷 3 s / 局刷 0.3 s / 刷新功耗 26.4 mW | **已 EOL**（官方替代型号 = `GDEY027T91`） | 尾货，**不选** |
| 微雪「2.7inch e-Paper」黑白（非 B） | —— | 参数与本项目手上的黑白规格书**逐项吻合**（3 / 1.5 / 0.42 s） | 在售 | 同源替代，可选 |
| ~~2.7" (B) 三色~~ | SSD1680Z8 | 15~17 s、**无局刷** | 现用 | **排除** |

**★ 两条决定性事实**

1. **`GDEY027T91` 的驱动 IC 就是 `SSD1680`** —— 与板上现用的 **SSD1680Z8 同族同型号**。⇒ 24P 脚序、`J6` 直插、泵配套（`L1`/`R14` 2.2 Ω/`Q1`/`D3–D5`）**全部沿用**，这正是 §12.4"泵够用"结论的硬证据（此前只是"同族推断"）。
2. **`GDEW027W3` 用的是 `EK79652`（天德钰）—— 另一家的 IC。** 它同为 2.7"/264×176/24P，但**IC 不同 ⇒ 脚序与泵配套必须重新核对**，不能因为"都是 24P"就买。

**淘宝/1688 搜索词（自购用）**

```
GDEY027T91
2.7寸 墨水屏 264x176 黑白 局刷
微雪 2.7寸 e-Paper 黑白 裸屏
中景园 2.7寸 墨水屏 局刷
```

**筛选红线 6 条**

| # | 规则 |
|---|---|
| 1 | 标题带 **"(B)" / "三色" / "红黑白"** ⇒ **排除**（那是 15~17 s、无局刷） |
| 2 | 分辨率必须 **264×176** —— `250×122` 是 2.13"，`296×128` 是 2.9" |
| 3 | **24 Pin FPC / 0.5 mm**，外形 **70.42 × 45.8 mm** |
| 4 | 要**裸屏**（raw display），**不要 HAT / 带驱动板的模块版**（那是走 `J1` 的另一条路） |
| 5 | **问客服 IC 型号**：优先 **SSD1680**；若答 **EK79652**，先索取规格书核脚序 |
| 6 | 确认**支持快刷 + 局刷**并问清秒数（应 ≈1.5 s / 0.3~0.42 s）；只写"3 秒刷新"而不提局刷的要警惕 |

**下单前两个动作**：① 让卖家发**规格书 PDF**（要能查到 IC 型号与三档刷新时间）；② 确认 FPC 出线方向与长度（本板 `J6` 为侧插、需折 180°）。
**建议买 2 片**：一片首件、一片备用（FPC 座反复插拔易松）。

## 12.6 FPC 机械核对：黑白排线"不是一条直排线"（2026-09-21）

**起因**：拿到候选黑白屏的结构图（用户截图 = **Waveshare 2.7" e-Paper 黑白**，与本仓 `Temp/ws27_bw_spec.pdf` 第 6 页**逐尺寸一致**），注意到**它的 FPC 不是一条直排线**。

### 12.6.1 图上的自证（两条）

1. 图纸引脚表：1=NC、2=GDR、3=RESE、…、24=VCOM，与本板 `J6` **逐脚一致**；图纸注明驱动 IC = **SSD1680**（与现用三色 SSD1680Z8 同族）⇒ 与本文件 §12.1/§12.5 的候选结论吻合。
2. FPC 尺寸（图纸原文）：**24 pin**、`P0.5×23=11.50±0.05`、阵列宽 **12.50±0.1**、**排线长 24.00±0.30**、排线厚 **0.14±0.03**（含补强 **0.3±0.03**）、末端**补强板 5.0±0.3**、有 **SILK LINE**（插入深度标记线）、标 **CONTACT SIDE**。

**轮廓确实带台阶**（非直排线）：图上另给 `4.15±0.3` / `3.65±0.3` / `7.54±0.30` / `0.28±0.05` / `0.30±0.08` 等台阶与偏置尺寸。

### 12.6.2 和现用三色屏的排线**不是一回事**（两份图纸实测）

| 项 | 2.7" (B) 三色（现用） | 2.7" 黑白（候选） |
|---|---|---|
| 图源 | `Temp/epd27_bv2_spec.pdf` p6 | `Temp/ws27_bw_spec.pdf` p6 |
| **排线长**（屏边→接触端） | **≈13.8 ~ 14.2 mm**（图上标 `14.210`，另有 13.151/9.47/3.66 链） | **24.00±0.30 mm** |
| 轮廓 | 宽根 **17.339** → 收窄到 12.50，带 `R0.900 / R2.000` 圆角，图上印 `FPC-7516 REV.B` | 台阶式；末端 **5.0±0.3 补强板** + SILK LINE |
| 阵列宽 / 脚数 / 间距 | 12.50±0.10 / 24 / 0.5 mm | 12.50±0.1 / 24 / 0.5 mm |
| 厚度 | 0.3 mm（含补强） | 0.14 排线 / **0.3 含补强** |

> 两处长度都**用各自图纸内的 12.50 mm 阵列宽做过像素标定**（黑白 4.0 px/mm、三色 12 px/mm），不是估读。
> ⇒ **差约 10 mm。** **"同为 24P、脚序相同"并不等于"排线一样"** —— 电气可插 ≠ 机械可装。

### 12.6.3 对 `J6` 的影响：分两层，一层无碍、一层要算

**① 电气 / 接触层：无影响。**
`J6` = **`C53055552`（FPC0509-24RL-TAG）**，立创商品参数实证：**触点类型 = 双侧触点 / 上下接**、**接入柔性电缆厚度 = 0.3 mm**、锁定 = 翻盖式（前插后掀）、板上高 1.05 mm。
- **双侧触点 ⇒ 排线接触面朝上、朝下都能插**，`CONTACT SIDE` 对 `J6` **不再构成约束**（这正是当初选它的理由）；
- 黑白图给的 **0.3±0.03** 正好落在座子"接入排线厚度 0.3 mm"上。

**② 机械层：要按"折返账"算，而账偏紧。**
本板实测（`Temp/r18_detail.txt`）：
```
屏投影        x[492,3248] y[1448,3259]   （70.4 × 46.0 mm，横放；排线从右短边引出）
J6 本体 bbox  x[3449,3602] y[1724,2282]
J6 插槽口     约 x=3603，距板边仅 138 mil (3.5 mm)
折返带（预留）x[3602,3745] × y[1600,2400]   （3.6 × 20.3 mm，已核净空）
```
⇒ 排线必须"**从屏右缘下来 → 横向绕到槽口之外 → 折 180° 回头插进槽**"。

| 账项 | 数值 |
|---|---|
| **可弯长度**（总长 − 末端补强段） | 黑白 **24.00 − 6.0 = 18.0 mm**（补强段**官方图 6.00±0.3**、微雪图 5.0±0.3 ⇒ 取保守值；见 §12.7.2）；三色 ≈ **9 ~ 11 mm** |
| 需要：横向绕行（屏右缘 3248 → 折返带外缘 3745） | ≈ **12.6 mm** |
| 需要：落差（排线从屏边到板面，支撑柱 5 mm 量级） | ≈ **5 mm** |
| 需要：折弯 180° + 插入深度 | ≈ **4 ~ 6 mm** |
| 直连（不走折返）时的一阶路径长 | `√(12.6² + 5²) ≈ 13.6 mm` + 折返/插入 ⇒ **≈ 18 ~ 20 mm** |

**判读**：黑白排线 ≈24 mm（可弯 19 mm）**与需求同量级、余量约 0~2 mm ⇒ 偏紧但可试**；三色排线（可弯 9~11 mm）在本走向下**明显不够**——说明**本板"屏的排线折返账"从来没按任何一款屏的实物排线核算过**（`J6` 与泵区原为 DNP 预留，此前的屏一律走 `J1` + 微雪驱动板模块）。**这是本轮新暴露的、必须在首件装配时验证的一项。**

### 12.6.4 ⇒ 由此新增一条**选型 + 验收**判据

**"出线位置"升级为硬指标。** 排线在 45.8 mm 短边上**偏置引出**，只有大致对着 `J6` 才能省下绕行长度：
- `J6` 槽口 y 中心 = **2003 mil**（体 bbox y[1724,2282]）⇒ 落在屏短边上 **距 y=1448 端 555 mil ≈ 14.1 mm** 处；
- 目标：**排线中心尽量对准这一点**；偏 3~4 mm 尚可，反向偏 9 mm（= 居中出线）会再吃掉约 9 mm 长度 ⇒ 直接判不够。

**下单前让卖家在结构图上标出三个数**：① 排线总长（屏边→接触端）；② 末端补强段长度；③ **出线中心到该短边端头的距离**。

**首件装配验收（新增）**
1. 不接电，先把排线试插 `J6`：应能把 **SILK LINE 插入线与槽口齐平**（说明插到位），锁盖能正常压合；
2. 排线**不许绷紧**：屏装到支撑柱后，排线应留 **≥2~3 mm 松弛**（绷紧会在锁盖上产生持续拉力，长期会拉伤焊点/铜箔）；
3. 折弯**必须落在台阶之后的柔性段**，**末端 5 mm 补强段绝对不可弯**；
4. 三色/黑白两片都试一次（若两片都买），确认哪一版走向更顺，作为后续固件/外壳的基准。

---

## 12.7 GDEY027T91 官方规格书逐项对表（2026-09-21）

> 源文件已留档 **`Temp/gdey027t91_spec.pdf`**（Dalian Good Display，39 页）。**结构图 p7 · 引脚表 p8 · 参考电路 p31 · 光学/可靠性 p27/p29。**

### 12.7.1 机械尺寸（本轮"对尺寸"的正题）

| 项 | 规格书 | 本板 / 本文件 | 判定 |
|---|---|---|---|
| 外形 | **45.80 (H) × 70.42 (V) × 1.0 (D)** mm | 投影 70.4 × 46.0；可用窗 95 × 46 | ✅ 一致。⚠️ **纵向只剩 0.2 mm 余量**（46.0 − 45.80）⇒ 外壳公差按此卡 |
| 厚度 | **1.0** mm | 本文件按 **1.5** mm 计入外壳总厚（≈33 mm） | ⚠️ 我们多留 0.5 mm ⇒ **外壳可再薄 0.5 mm**（不改板） |
| 有效区 | **38.19 × 57.29**（横放 = 57.29 × 38.19） | 264 × 176 横向 | ✅ |
| 点距 / DPI | 0.217 × 0.217 / **117** | 与三色同点距（DPI 118） | ✅ |
| 重量 | **5.5 ± 0.5 g** | — | 新增（配重/结构件可用） |
| 边缘余量 | **3.82 + 38.19 + 3.79 = 45.80** | — | 新增（有效区几乎居中） |

### 12.7.2 FPC（衔接 §12.6）

| 项 | 规格书 p7 | 微雪 2.7" 黑白图纸 | 判定 |
|---|---|---|---|
| 脚数 / 间距 / 阵列宽 | 24P / 0.5 / **12.50 ± 0.05**（`P0.5×23=11.50±0.05`） | 同 | ✅ |
| **排线长** | **24.00 ± 0.30** | 同 | ✅ **两份逐项同值 ⇒ 同一版排线设计** |
| 台阶尺寸 | 4.15±0.3 / 3.65±0.3 / 7.54±0.30 / 0.28±0.05 / 0.30±0.05 | 同 | ✅ 同一版 |
| **末端补强段** | **6.00 ± 0.3** | **5.0 ± 0.3** | ⚠️ **两份不同（差 1 mm）⇒ 可弯长度取保守值 = 24.00 − 6.00 = 18.0 mm** |
| 厚度 | 0.14 ± 0.03（裸）/ **0.3 ± 0.03（含补强）** | 同 | ✅ 正好落在 `J6` 要求的 0.3 mm |
| 标记 | SILK LINE / CONTACT SIDE | 同 | ✅ |
| **沿 45.8 mm 边的出线偏置** | **图纸未标注** | 未标注 | ⚠️ **只能实测**（见 §12.6.4；实物到手用卡尺量 30 秒） |

### 12.7.3 电气 / 温度 / 固件约束

| 项 | 规格书 | 本板 · 本文件 | 判定 |
|---|---|---|---|
| 刷新 | 全 **3 s** / 快 **1.5 s** / 局 **0.42 s**（25 ℃） | §12.5 原写"局刷 0.3 s（佳显官网）" | ⚠️ **以规格书 0.42 s 为准**（已更正） |
| 工作 / 存储温度 | **0 ~ +50 ℃ / −25 ~ +70 ℃** | — | ✅ 存储比三色（−25~+40）**宽 30 ℃** |
| VCI / VDD | VCI 3.0 V；VDD 1.7 / 1.8 / 1.9 V | 本板 VCI = 3V3 ✅、VDD 挂 C51 ✅ | ✅ |
| 睡眠 | **sleep 20 µA（保持 RAM）/ deep sleep 1~5 µA（RAM 不保持）**、deep sleep 功耗 0.003 mW | — | ⚠️ **固件：深睡丢显存** ⇒ 唤醒必须重写帧；要保显存只能进 sleep |
| BS1 | L = 4 线 8 bit SPI；H = 3 线 9 bit | 本板 BS1 = L（4 线）✅ | ✅ |
| 驱动 IC | **SSD1680**（p7 注 6） | 与板上 SSD1680Z8 同族 | ✅ |

### 12.7.4 参考电路（p31）与本板泵区逐件对表

| 件 | 规格书参考电路 | 本板 | 判定 |
|---|---|---|---|
| 升压 NMOS | `Q1` **Si1308EDL**（Vds≥30V / Vgs(th)≤1.5V / **Rds(on)≤400 mΩ**） | `Q1` **AO3400A**（Rds ≈ 30 mΩ） | ✅ 本板远优 |
| 升压二极管 | **MBR0530 ×3** | `D3–D5` MBR0530 ×3 | ✅ 一致 |
| **RESE 检流电阻** | **`R2` = 2.2 Ω, 1%** | `R14` = **2.2 Ω ±1%**（C17521） | ✅ **三份独立证据一致**（本规格书 p31 + SSD1680 手册表 13-1 + 微雪 2.7" 规格书 p20） |
| 栅极下拉 | `R1` = 1 MΩ | `R29` = 10 kΩ | ✅ 均可（本板更强） |
| 飞跨电容 | `C3` = 4.7 µF / 25 V | `C26` = 4.7 µF / **50 V** | ✅ 值同、耐压更优 |
| 各轨 1 µF | `C2、C5–C12`（每轨一颗，25 V） | `C17–C28` = 1 µF / **50 V** | ✅ 数量与轨位一致、耐压更优 |
| 输入 bulk | `C4` = 4.7 µF / 25 V | 本板 10/22 µF 级 | ✅ 更充裕 |
| FPC 座 | `P1` 24P / 0.5 mm | `J6` 24P / 0.5 mm | ✅ |
| **升压电感 `L1`** | **47 µH / 500 mA**（2026-09-21 11× 放大复核：**47µH**，上一版读成 4.7µH 系低分辨率误读；同页元件表另注 *refer to NR3015: Io = 500 mA(max)*） | **68 µH，380 mA** | ⚠️ **两份官方一致指向 47µH/500mA，本板 68µH 反而是唯一的例外** |

**★ `L1` 取值对照（2026-09-21 复核后修正版）**

| 出处 | 感量 | 电流 |
|---|---|---|
| **SSD1680 手册 Table 13-1**（IC 原厂） | **47 µH**（参考 CDRH2D18 / LDNP-470NC） | Io ≤ **500 mA** |
| **GDEY027T91 参考电路 p31**（面板原厂） | **47 µH** | 500 mA |
| **本板**（= 微雪 2.7" HAT 实测值 `Temp/ws27_hat_schematic.pdf`） | **68 µH** | **380 mA**（LCSC 口径） |

- ⚠️ **上一版写"面板参考 4.7µH / 三方三个值"是误读**：2026-09-21 把该处放大 **11×** 重看，标注清楚是 **`47uH 500mA`** ⇒ **两份官方（IC + 面板）完全一致 = 47µH / 500mA**，**本板 68µH 是唯一不同的那个**（来源是微雪 HAT，不是官方推荐值）。
- SSD1680 手册仍注明"推荐值随面板负载变化、客户须自行复核"，所以 68µH 未必不能工作；但**我们的电感电流上限只有官方设计点的 76%（380/500）**，这是本板泵区**唯一可量化的余量缺口**。
- **首件必测**：`L1` 温度、刷新时间（应 ≈ 3 s）、VGH/VGL 建立；若泵力不足或 `L1` 明显发热 ⇒ 按 §12.7.6 换料。

### 12.7.6 `L1` 换料候选：同焊盘（3.5×3.0）里能换什么
> ### ✅【2026-09-21 已实施】`L1` 换料已完成，原理图侧生效
> - 活体操作记录：`schematic.component.replace`（primitiveId `f35459c8529f3ccd` → 新 `4cecaacf22840b1b`，**uniqueId `gge79` 保留**）⇒ `easyeda sch modify --patch-file` 补齐 10 项属性。
> - 终验：`supplierId=**C2826626**` / `manufacturerId=CD32-470M 47UH` / `Value=**47uH**` / `Current Rating=**470mA**` / `DCR=**1.25Ω**`；位置 (1135,1270) 与 rotation 未变；`sch drc` = **0 fatal / 0 error**、`sch check` = **0 悬空脚 / 0 断线**。
> - ⚠️ **换绑过程踩到的坑（已修复）**：`replace` 之后新 device 的 `otherProperty` 全为空；补属性的那次 `modify` 又把 **`supplierId` 侧写坏成 `CD32-470M.1`**（正是历史记过的"第二次 modify 侧写"现象）⇒ 用最小 patch `{"supplierId":"C2826626"}` 修回。**教训：换型号必须"replace → 一次性补全属性 → 单独修 supplierId → 终验全字段"。**
> - ✅ **PCB 端已闭环（2026-09-21）**：先在 EasyEDA 对话框把原理图绑进板子（`Board1` 原 `schematicUuid=null` ⇒ 选对话框**第二项 `Schematic1`**（= 我们的原理图文档）修复，**绝不能选默认的「新建原理图…」**），导入随即生效 —— 器件变为 `CD32-470M 47UH`（`supplierId=C2826626`、`uniqueId=gge79` 保留、位置 (594,956) 未变），件数仍 **121**。
> - ⚠️ **导入带出的副作用（已修）**：换器件时原理图符号引脚移位 ⇒ **L1 两脚网络被对调**（左盘标 `$1N324`、右盘标 `3V3`，与两侧铜箔相反）⇒ `pcb drc` 报 **12 条**（6× `SMD Pad to Track` 距 0 + 4× `Connection Error` + 2× 焊盘/过孔）。**修法：PCB 上把 L1 旋转 180°** —— 该器件两焊盘关于原点严格对称（±44.3mil），旋转后**铜箔几何逐点不变**，只把两盘编号与两侧网络重新对上；位号再 `silk-set --rotation 0` 转正、`silk-align --refs L1` 避开 U10 焊盘。
> - **换料后实测焊盘**（活体）：**2 盘 / 中心距 88.6mil / 各 49.2×118.1mil**（原 51.2×122.8 / 90.6mil）——新 device 官方 land pattern 略小：**内缘未动、外缘短 2mil、两盘各内缩 1mil**，走线仍完整落盘。
> - **终验**：`pcb drc` = **passed / 0 违规**；DFM **504 条 / ERROR=8**（与基线同为椭圆焊盘外接矩形伪报）、`silkFlipped=0`、`silkOverPad=171`；铺铜实铺 **L1 46.56% / L2 54.88%** 与换料前逐位一致；工作流阶段门已重建（placement/outline/pre_route 已确认、`routing: ✅ authorized`）。
> - **教训（跨项目）**：**"同厂同系列同封装 ⇒ 换料只需改料号"这个假设在"引脚网络"这一层会失效** —— IC/器件符号的引脚位置一变，导入后原理图网表侧就可能把两个脚对调；**对称二端器件（电感/R/C）用"旋转 180°"就能零铜箔代价修回**，不对称器件则要回改原理图接线。换料后**必须**跑 `pcb drc` 并**逐盘比对 pad 的 net/坐标**，只看"焊盘几何一致"会漏掉这类错。
> - 📌 **全文口径**：本文其余各处写「本板 `L1` = 68 µH / 380 mA / C2826627」的段落（§12.4 泵能力、§13.2、§14.5 等）均为 **2026-09-21 换料前**的记录，现状一律以本节为准 ⇒ **`L1` = 47 µH / 470 mA / C2826626（PCB 上 rotation=180°）**。
> - 📌 **口径更正**：BOM 09-14 那条"泵峰值 <100mA ⇒ 余量 4 倍"把**负载电流**当成了**电感峰值**；boost 电感峰值由 RESE 限流点决定（官方按 500mA 设计）⇒ 380mA 偏紧，这才有本轮换料。


**本板 `L1` 的实测焊盘**（**换料前的原器件**，活体 `pcb dump --doc PCB1`）：器件 `CD32-680M 68UH`（DMBJ 振宝佳，**C2826627**），封装 `IND-SMD_L3.5-W3.0`，
**2 个焊盘、中心距 90.6 mil = 2.30 mm、每个 51.2 × 122.8 mil（1.30 × 3.12 mm）**；DCR 1.82 Ω。

**同封装族（3.5×3.0）候选**（LCSC 口径"额定电流"，同一数据源可横向比较）：

| 型号 | LCSC | 感量 | 额定电流 | DCR | Isat | 判定 |
|---|---|---|---|---|---|---|
| **`CD32-470M 47UH`（DMBJ）** ⭐ | **C2826626** | **47 µH** | **470 mA** | **1.25 Ω** | — | **首选**：同厂同系列同体 3.5×3.0；感量 = 两份官方推荐值；电流 +24%、DCR −31% |
| `CD30D22HF-270MC`（Sumida） | C2454484 | 27 µH | **580 mA** | — | **650 mA** | 同尺寸里**唯一 ≥500mA**，但感量只有推荐的 57% |
| `CD32-220M`（Zoomdo） | C49230784 | 22 µH | 630 mA | — | — | 电流够，感量仅 47% |
| `SLF0302-220MTT`（Sunltech） | C181845 | 22 µH | 720 mA | — | — | 同上 |
| `PCD0302KT470`（Viking） | C324132 | 47 µH | 330 mA | — | — | 封装名与本板**完全一致**，但电流更低 ✗ |
| 现用 `CD32-680M`（DMBJ） | C2826627 | 68 µH | 380 mA | 1.82 Ω | — | 现状 |
| ~~`SLF0302-680MTT`~~ / ~~`SMTDR32-680M`~~ / ~~`SM3521-680M`~~ | C411914 / C326200 / C241167 | 68 µH | 180 / 260 / 160 mA | — | — | 同名封装但电流更差 ✗ |

**需要改焊盘的选项**（**代价：动器件 ⇒ 布局 pose 指纹失效 ⇒ 必须重跑 `confirm-layout` + 复检布线/DRC**，非必要不做）：

| 型号 | LCSC | 体尺寸 | 感量 | 额定 / Isat |
|---|---|---|---|---|
| `ZENR4018T470M`（ZE） | C42428007 | 4.0×4.0 | 47 µH | 420 mA / **570 mA** |
| `CKCS5020-47uH/M`（岑科） | C354600 | 5.0×5.0 | 47 µH | **750 mA / 770 mA** |

**结论与建议**
1. **首选零改动换料：`CD32-470M`（C2826626）** —— 感量正好落到两份官方推荐的 **47µH**，电流 380→**470mA（+24%）**、DCR 1.82→**1.25Ω（−31%，泵发热更低）**；同厂同系列同体 ⇒ 焊盘同一份规格书，**大概率真 drop-in**（换料号即可，不动板）。
2. **不建议为凑"≥500mA"去牺牲感量**：同尺寸达标的只有 27µH / 22µH 档。固定峰值限流（RESE 决定）下每周期能量 ≈ ½LI²f ⇒ **感量降到 47% 会同步削弱泵的输出能力**，而在低温 / 满屏翻转时正是泵最吃力的场景；差的那 30mA（470 vs 500）不值得拿这个换。
3. **要真正的 ≥500mA 且不牺牲感量** ⇒ 只能上 4×4 / 5×5 ⇒ **必须改焊盘**，属于 V2 或返工决策，先别做。

**★ `470 mA` 与官方 `500 mA` 的口径差（2026-09-21 追加）**

- **不是一个尺子**：官方的 500 mA 是**电流等级**（Table 13-1：`L1  Io = 500 mA (Max)`、`D1–D3  Io ≥ 500 mA`、`Q1  ID > 500 mA`），意思是"这一族功率级按 500 mA 档选件"，**不等于泵实际会跑 500 mA**；而 LCSC 给国产功率电感的"额定电流"通常是**温升电流（Irms，ΔT 口径）**，且本器件感量公差为 **±20 %**（官方表写 ±10 %，与旧件 CD32-680M 同一档，换料未变差）。⇒ **6 % 之差只在"额定值"这一层面成立**。
- **改用"同发热"来比，结论更有用**：额定点的铜损 = I²·DCR ——
  - 旧 `CD32-680M`：0.380² × 1.82 Ω = **0.263 W**
  - 新 `CD32-470M`：0.470² × 1.25 Ω = **0.276 W**
  ⇒ **两者的额定点功耗几乎相同（≈0.27 W，即"温升到规范值"的那个点）** ⇒ **新件在同发热下多给 24 % 电流**，这才是它敢标 470 mA 的原因（不是"标得虚")。
- **余量账**：380 → 470 mA = **+24 %**；对 500 mA 的缺口由 **−24 % 收窄到 −6 %**。这 6 % 与下列不确定度同量级 ⇒ **纸面算"追平"，不是"还差一截"**：感量/DCR 各 ±20 % 公差 · LCSC"额定电流"与厂商 Isat 的口径差 · 面板批次容性负载差 · 温度降额曲线未给。
- **整条功率级只有电感还在 500 mA 线下**：`MBR0530` = 500 mA ✅、`AO3400A` = 5.7 A ✅、`R14` 只负责采样 ⇒ 换到 470 mA 后它与全链**同档**（旧 380 mA 是明显短板）。
- ⚠️ **（已被下节"二次核查"修正）** 原推测"2.2 Ω ⇒ 峰值 ≈0.5 A"缺少手册依据 —— 官方参考件的 **Isat 只有 200 mA**，说明这套泵的工作峰值应 ≲200 mA，**0.5 A 更像保护阈值而非工作点**。

**★ 二次核查：官方参考电感的实物规格（2026-09-21，修正上一节）**

上一节的 500 mA 取自 IC 手册表里的整数。查了**那颗参考件的实物规格书**（Sumida `CDRH2D18/LDNP-470NC`，立创 `C2454210`；来源 = Sumida 官方 datasheet + JLCPCB 参数页，A/B 级）：

| 参数 | **官方参考件** CDRH2D18/LDNP-470NC | **本板新件** CD32-470M | 本板旧件 CD32-680M |
|---|---|---|---|
| 感量 | 47 µH **±30 %** | 47 µH ±20 % | 68 µH |
| **温升电流**（ΔT = 40 °C） | **480 mA**（20 °C）/ 380 mA（100 °C） | **470 mA**（LCSC「额定电流」同口径） | 380 mA |
| **饱和电流 Isat** | **仅 200 mA**（20 °C；定义为感量降到 65 % 时）/ 150 mA（100 °C） | 未标注（LCSC 只给额定电流） | 未标注 |
| **DCR** | **530 mΩ typ / 660 mΩ max** | **1.25 Ω** | 1.82 Ω |
| 体积 / 形式 | 3.2 × 3.2 × 2.0 mm，**屏蔽** | 3.5 × 3.0 × 2.0 mm，非屏蔽 | 同左 |

**结论一（口径校正）**：`500 mA` 与 `470 mA` **是同一把尺子（温升电流）**，而官方实物的真值是 **480 mA** ⇒ **我们与官方差 2 %，不是 6 %**（470/480 = 98 %）。

**结论二（推翻上一节的峰值推论）**：官方敢用一颗 **Isat 只有 200 mA** 的件做参考设计 ⇒ **这套泵的正常工作峰值电流必然 ≲200 mA**（否则参考设计会长期工作在饱和区）。所以：
- 上一节"2.2 Ω ⇒ 峰值 ≈0.5 A ⇒ 380/470 mA 两种电感都在超标区工作"的判断**过于保守、作废**；
- ✅ **已结清（2026-09-21，拿到 DMBJ 原厂规格书，留档 `Temp/cd32_470m_ds.pdf` p9）**：关于"混口径"的撤回仍然成立（**不能拿额定电流直接比 Isat**），但结论从"未知"升级为**有界的正面结论** —— 厂商**不单列 Isat**，而是把 `IDC (A) max` 定义为**两个判据中较小者**：
  > *"The maximum rated current is a DC current which causes initial inductance to decrease by **25 %** or temperature to rise by **40 ℃**, **whichever is smaller** (at ambient 20 ℃)."*
  ⇒ `0.47 A` 的含义 = **在 470 mA 下，感量降幅 ≤25 % 且温升 ≤40 ℃（两项同时满足，至少一项刚好到限）**。据此：
  - **感量 / 饱和判据**：官方参考件 `Isat = 200 mA` 是**降到 65 %（降幅 35 %）**的电流；本板件在 **470 mA** 时降幅仍 **≤25 %**（剩余 ≥75 %）⇒ 同一电流下本板件**剩余感量更高，饱和裕度明显优于官方参考件**；
  - **温升判据**：官方 480 mA vs 本板件 **≥470 mA**（470 是"取小者"，温升那一路必然 ≥470）⇒ **与官方持平（最坏差 2 %）**。
  - ⇒ **两项判据都不劣于官方参考件**（饱和裕度更优、温升持平）；**旧版拿"额定电流 vs Isat"直接比仍是错的**，只是现在能把两项分别摆平了。
  - 📎 同族"要更大余量"的 47 µH 选项（**都要改焊盘 ⇒ V2**）：`CD43-470M` = 0.54 A / DCR 0.844 Ω / 4.5×4.0 mm；`CD54-470M` = 0.72 A / 0.37 Ω / 5.8×5.2 mm。

**结论三（换料的真实收益，重新表述）**：① 感量对齐官方闭环工作点（68 → 47 µH）；② DCR 1.82 → 1.25 Ω（**发热 −31 %**）；③ 电流档 380 → 470 mA（对官方实物 480 mA 从 **79 % 提到 98 %**）。
**仍然不如官方的一处**：**DCR（1.25 Ω vs 530 mΩ）与非屏蔽形式** —— 它带来的是"泵区略热 / 漏磁略大"，**不是"电流不够"**。

**遗留的未知（已收窄到一条）**：规格书只给出 `≤25 % 降幅 @470 mA` 这**一个点**，**感量降幅 vs 电流的整条曲线仍未知**（也无法确知 470 mA 到限的是"感量"还是"温升"哪一路）；并且 `L1` 的实测峰值电流无人量过。**首件用示波器看 SW 节点峰值平台**即可收口。
**另记两处规格书与商城口径差异**：① 工作温度 **−25 ~ +85 ℃**（规格书 p9；LCSC 页面写 −40~+85，**以规格书为准**）；② 感量测试条件 **1 kHz / 0.3 V**（非 100 kHz），与 Sumida 参考件的 100 kHz 不同口径，横向比感量本身时留意。

**★ 附带一条通用结论：屏蔽 vs 非屏蔽 —— DCR 与饱和是一对反比（2026-09-21 查证）**

同感量、同尺寸下，**磁屏蔽（闭合磁路）件的 DCR 通常更低** —— 机制不是"屏蔽"这个功能本身，而是**磁路闭合 ⇒ 磁阻低 ⇒ A_L 高 ⇒ 同样感量只需更少匝数 ⇒ 线更短更粗**。厂商口径一致：
- Würth Elektronik《Power Inductors 8 Design Tips》Tip 4 / Tip 6："*same inductance for a shielded inductor – lower DC resistance*"、"*higher A_L value, therefore lower DC resistances for the same inductance*"；
- GOTREND 技术文：闭合磁路磁阻极低，"*非屏蔽件在**同体积同匝数**下做出同样感量时 DCR 更高*"；
- Power's Wiki：「同电感值，屏蔽件比非屏蔽件 DCR 更小」。

**但代价是"更容易饱和"** —— 本板那两颗正好是教科书级的对照：

| 47 µH，相近体积 | **屏蔽**（Sumida CDRH2D18/LDNP-470NC，3.2×3.2×2.0） | **非屏蔽工字**（DMBJ CD32-470M，3.5×3.0×2.1） |
|---|---|---|
| DCR | **530 mΩ typ / 660 mΩ max** | **1.25 Ω** |
| 感量/饱和判据 | **Isat 仅 200 mA**（降到 65 %） | **IDC 470 mA**（降幅 ≤25 %） |
| 温升额定 | 480 mA | ≥470 mA |

⇒ **DCR 低 1.9 倍，但饱和裕度差 2.3 倍以上**。真正同时拿到"低 DCR + 高饱和 + 屏蔽"的是**一体成型（金属粉芯模压）**（分布气隙 + 高 Bsat + 宽端子，DCR 档位 1~20 mΩ），典型如本板 `L3` = WPN4020H1R5MT（1.5 µH / 36 mΩ / Isat 7.7 A / 4×4×2）；代价是**贵 + 小尺寸做不出大感量** ⇒ **3.5×3.0 里的 47 µH 买不到一体成型**，只能在"工字非屏蔽"与"铁氧体屏蔽"之间二选一。
⚠️ **反例提醒**：屏蔽罩本身占体积，低档系列里屏蔽件的 DCR 可能反而更高（Würth 自家 0805 替料就出现过 180 mΩ 非屏蔽 vs 100 mΩ 屏蔽之外的其它组合）⇒ **只在同一系列内比，别凭"屏蔽"两个字跨系列下结论**。

**对本板的取舍判定**：EMI 敏感或紧邻敏感电路 ⇒ 优先屏蔽；但**boost 泵属于"怕饱和、不怕几欧 DCR"的场合** —— 毫瓦级负载下 DCR 多 0.72 Ω 只值几毫瓦（按 100 mA RMS 算 +7 mW，200 mA 才 +29 mW），而**饱和是失效模式**（感量崩 ⇒ 峰值电流失控 ⇒ 管子/电感受损）⇒ **本板"宁要饱和裕度、忍一点 DCR"的方向是对的**。

### 12.7.5 本轮顺手修掉的两处文档不一致

1. §12.5 的"局刷 **0.3 s**" → **0.42 s**（规格书 p10 口径）。
2. `docs/SCHEMATIC_CHECKLIST.md`：**2026-09-13 的"R14 固定 0.47 Ω"结论已作废**（2026-09-16 已改为 2.2 Ω，BOM.md 有改值记录与料号；本轮又获规格书 p31 的第三份独立证据）。文件内其余 "R14 0.47 Ω" 字样一律按 **2.2 Ω** 读。

---

## 十三、RESE（泵限流/反馈电阻）的容差与验证

> 来源：**大连佳显 good-display 官方技术文章**（《Explanation of E-ink Driver Circuit》《Parameters of SPI E-paper Display Hardware》，B 级＝屏厂参考设计）+ GDEW029T5D 规格书。
> 触发：实测「把采样电阻由 2.2 Ω 改到 3 Ω，2.13" 屏仍正常刷新」。

### 13.1 RESE 的真实角色 —— 是**闭环反馈采样点**，不是死值

佳显原文（译）：
> 外部升压需要升压电感、N-MOS 与必要电容。**GDR 信号由 IC 内部产生**，控制 N-MOS 开关以完成电感/电容的充放电。**RESE 电阻采样电流回传给 IC，形成闭环反馈信号，保证升压电路稳定输出**。升压电路提供 **PREVGH / PREVGL** 两种高压，IC 再由内部降压得到 VSH / VSL 供 E Ink 膜。

⇒ IC 的**目标始终是"把 PREVGH/PREVGL 稳到目标值"**。RESE 变化只改**环路工作点**（限流阈值），不改目标。所以它天然是"带容差的推荐值"。

### 13.2 两类驱动 IC 的官方配套值

| 项 | **Ultrachip 系列 (UC)** | **Solomon 系列 (SSD)** |
|---|---|---|
| 电感 | **10 µH ±10%（Io ≤ 1 A）** | **47 µH ±10%（Io ≤ 500 mA）** |
| N-MOS | Si1308EDL（VDS>25V / ID>500mA / VGS(th)<1.5V / Rds(on)<400 mΩ） | 同左 |
| 二极管 | MBR0530 | 同左 |
| **RESE** | **0.47 Ω 1 %** | **2.2 Ω 1 %** |

⇒ `0.47 Ω` 与 `2.2 Ω` 各自与**其 IC 内部参考电压**配套，**不能跨族套用**（换族要同时换 RESE）。
⇒ 本板：**R14 = 2.2 Ω（SSD 族正确值）**、**L1 = 68 µH**（微雪驱动板同源值，比佳显推荐的 47 µH 大；电流纹波更小，闭环可适应）—— 外围值本身就有工程余量。

### 13.3 方向性（最重要的一条）

| 改动方向 | 峰值电流 | 后果 | 定性 |
|---|---|---|---|
| **RESE 变大**（2.2 → 3 Ω） | 变小 | 泵输出能力下降 ⇒ 建立变慢、带载变弱；**只要仍够用就"看不出问题"** | ✅ **安全方向** |
| **RESE 变小**（2.2 → 1 Ω） | 变大 | 可能超电感 Io 上限（SSD 族 500 mA）⇒ **电感饱和、N-MOS 过热、效率崩、EMI 变差** | ⚠️ **危险方向** |
| **RESE 开路（未焊）** | 无反馈 | **升压直接起不来**（佳显列为"升压失败的五大原因"之一） | ❌ **不可省** |

### 13.4 为什么 3 Ω 仍能正常刷新

两条同时成立：**① 改的方向是安全的**（限流点下移）；**② 负载有余量** —— 2.13" 屏像素少、面积小，泵的容性负载本来很轻，**电流根本达不到限流点** ⇒ 轻载下 RESE 几乎不参与工作。

所以这个实验结果完全合理：它说明 **RESE 容差比想象中宽**，而不是"RESE 可以随便取"。

### 13.5 ⚠️ 但「能刷新」不是灵敏判据

轻载＋室温＋满电时泵余量最大，最容易"看起来没问题"。真正的判据是**边界条件**：

| 边界条件 | 为什么最考验泵 |
|---|---|
| 电池低压（VCI 2.2~3.0 V） | 升压比最大、占空比最极限 |
| 低温（0~5 °C） | 电泳迁移率与 IC 效率双降 |
| 满屏翻转（全黑 ↔ 全白） | 容性负载最大、瞬态电流最高 |
| 连续快速刷新 / 局刷 | 泵持续工作、无恢复间歇 |
| 更大尺寸屏（2.7" / 2.9"+） | 面容量随面积增长 |

**验证法**：在上述条件组合下看**刷新时间是否明显变长、对比度/残影是否变差**；有条件用示波器看 PREVGH/PREVGL 建立过程与峰值电流平台。

### 13.6 对本板的结论

- **R14 = 2.2 Ω 保留不动** —— 2.7" 三色与 2.7" 黑白同属 SSD 族，规格值相同。
- 2.2 → 3 Ω 的实测说明容差至少有 **+36 %** 余量 ⇒ **换黑白屏时 RESE 不再是顾虑点**。
- **不要往小改**（1 Ω 级）—— 那是唯一可能烧电感/N-MOS 的方向。
- ⚠️ 另提醒：**「24P FPC」不等于「脚序相同」** —— 同为 24P 的 GDEW029T5D（2.9"）其 pin 4/5 是 VGL/VGH，而 Waveshare 2.7" 的 pin 4/5 是 NC/VSH2 ⇒ **换任何非同型号屏，都必须核对该屏自己规格书的脚序**。

## 十四、驱动器原厂配套表与 RESE「甜点值」

> **A 级来源**：Solomon Systech **SSD1680 datasheet Rev 0.14 / Jun 2019**（46 页，本地 `Temp/ssd1680_ds.pdf`）§6.3 Booster & Regulator、§11 Electrical Characteristics、§13 Application Circuit。

### 14.1 原厂 application circuit 元件表（Table 13-1，逐项照抄）

| Part | Value | 原厂要求 / 参考件 |
|---|---|---|
| C0–C1 | 1 µF | X5R/X7R；耐压 **6 V 或 25 V** |
| C2–C7 | 1 µF | 0402/0603/0805；X5R/X7R；耐压 **25 V** |
| C8 | 0.47 µF / 1 µF | 0603/0805；X7R；25 V。**注：18 V 直流偏压下有效容值 > 0.25 µF** |
| **R1（RESE）** | **2.2 Ω** | 0402/0603/0805；**精度 1 %**，**≥ 0.05 W** |
| D1–D3 | MBR0530 | 反压 ≥ 30 V；**Io ≥ 500 mA**；Vf ≤ 430 mV |
| Q1 | NMOS：Si1304BDL / NX3008NBK | VDS ≥ 30 V；Vgs(th) = 0.9 V typ / 1.3 V max；**Rds(on) ≤ 2.1 Ω @ Vgs = 2.5 V** |
| **L1** | **47 µH** | CDRH2D18 / LDNP-470NC；**Io = 500 mA (Max)** |
| U1 | 0.5 mm ZIF socket | 24 pin / 0.5 mm pitch |

**★ 原厂 Remarks（关键，等于官方承认"不是死值"）**：
> 1) Table 13-1 的推荐元件值与参考件**会随面板负载而变化**（subject to change depending on panel loading）。
> 2) **客户需自行评估所选元件值与器件是否适合其应用。**

### 14.2 IC 内部稳压器能力（Table 11-2）——决定"负载有多轻"

| 项 | 值 |
|---|---|
| VSH1 输出电流能力 | **800 µA** |
| VSH2 输出电流能力 | 800 µA |
| VSL 输出电流能力 | **800 µA** |
| VCOM 输出电流能力 | **100 µA** |

⇒ 屏的**稳态驱动电流是 µA 级**，泵的负担极小；真正吃电流的只有刷新瞬间的容性负载。这也印证了 §13 的结论：轻载时 RESE 几乎不参与工作。

### 14.3 典型输出电压（Table 11-1 的测试条件）

| 轨 | Typ | Min / Max |
|---|---|---|
| VGH | +20 V | 19.5 / 20.5 V |
| VSH1 | +15 V | 14.8 / 15.2 V |
| **VSH2** | **+5 V** | 4.9 / 5.1 V |
| VSL | −15 V | −15.2 / −14.8 V |
| VCOM | −2 V | −2.2 / −1.8 V |

⚠️ 两处要注意：① 上表是**测试条件**下的典型值；**实际各轨电压由屏厂 OTP 波形决定**（屏规格书实测 VGH = +22 V / VGL = −20 V，比此表更高）；② **`VSH2` 的原厂条件只有 +5 V** —— 所以挂在 VSH2 脚上的 `C28` 耐压要求其实很低（本板已用 50 V，余量极大）。

### 14.4 「甜点值」到底在哪 —— 窗口是不对称的

**甜点 = 原厂推荐值本身 = 2.2 Ω。** 理由：`I_peak ≈ V_ref / R1`（V_ref 是 IC 内部阈值），而 2.2 Ω 正是**让峰值电感电流刚好用到配套件能力（L/D 的 Io = 500 mA）**的那个点。

```
危险区  ←──  2.2 Ω  ←──────────  可用区（泵能力渐降）  ──────────→  升不起来
              ▲
        原厂推荐点（贴近窗口下沿）
```

| 偏离方向 | 后果 | 结论 |
|---|---|---|
| **往大**（2.2 → 3 → 4.4 Ω…） | 峰值电流按 1/R 下降 ⇒ 泵能力下降；负载轻时看不出 | ✅ **窗口宽**（实测 +36 % 正常） |
| **往小**（2.2 → 1 Ω…） | 峰值电流翻倍 ⇒ 超电感/二极管 500 mA 额定 ⇒ 发热、效率降、波形畸变 | ⚠️ **窗口窄**（推荐值已贴近下沿） |
| 开路 | 无反馈 ⇒ **升压起不来** | ❌ |

**"甜点区"的实用判据**（不用示波器）：
- 找**下界**（带不动）：低压（VCI 下限）＋ 低温 ＋ 满屏翻转，看刷新是否变慢/变淡；
- 找**上界**（饱和）：往小调时摸 MOS/电感温度、看画面是否出现条纹或对比度异常。
- 两点之间的中点就是"甜点区"，但**工程上仍取原厂 2.2 Ω 最稳**。

### 14.5 本板配套件 vs 原厂推荐（逐项对照）

| 位号 | 原厂推荐 | 本板实装 | 判定 |
|---|---|---|---|
| **R14（RESE）** | 2.2 Ω / 1 % / ≥0.05 W | **2.2 Ω 0805 ±1 %** | ✅ 完全一致 |
| **L1** | 47 µH / Io ≤ 500 mA | **68 µH**（微雪驱动板同源值） | ✅ 更大电感 ⇒ 电流纹波更小 |
| **Q1** | Si1304BDL / NX3008NBK（**Rds(on) ≤ 2.1 Ω** @2.5 V） | **AO3400A**（Rds(on) ≈ **30 mΩ** 量级） | ✅ **远优于推荐**，开关损耗更低 |
| **D3–D5** | MBR0530（反压≥30 V、Io≥500 mA、Vf≤430 mV） | **MBR0530** ×3 | ✅ 完全一致 |
| 泵电容 | C2–C7 = 1 µF / **25 V**；C8 要求 18 V 偏压下有效容值 >0.25 µF | 1 µF / **50 V**（高压侧已统一 C28323） | ✅ 高于原厂要求（直流偏压缩水更少） |

**结论：本板的泵区在四项配套件上全部达标或优于原厂推荐 ⇒ RESE 无需改动。**

> ⚠️ 想提升泵能力时**不要靠减小 RESE** —— 那只会让电感先饱和。正确做法是升级整套（更大 Io 的电感/二极管、更低 Rds(on) 的 MOS），或换 IC 族（UC 族电感上限为 **1 A**，泵能力更强）。

---

## 十五、三颗 Solomon 驱动器对比与本板泵的驱动能力评估

> 触发：SSD1619A / SSD1683 / SSD1680 这三颗 IC，本板泵区能否驱动？
> 来源：**SSD1680 datasheet Rev 0.14（A 级，本地 `Temp/ssd1680_ds.pdf`，含完整元件表）**；SSD1683 / SSD1619A 的 **规格书目录与引脚表**（B 级，来自官方 PDF 检索结果）；**点墨电子/GoodDisplay 官方技术文**《电子纸外部 IC 升压电路》（B 级）。
> ⚠️ SSD1683 与 SSD1619A 的 **§13 Table 13-1 元件表数值本次未取到**（本机直连网络被拒 + 站点 Cloudflare 挑战），下表对应项以"同族推断"标注。

### 15.1 三颗 IC 的共性 —— 都**必须外接泵**，且**接口完全一致**

| 项 | SSD1680 | SSD1683 | SSD1619A |
|---|---|---|---|
| 分辨率 | 176 src × 296 gate | **400 × 300** | **400 × 300** |
| 颜色 | 黑白红 | 黑白 + 黑白红 | 黑白红 |
| VCI / VDD | 2.2~3.7 V / **1.8 V（内部稳压）** | 2.2~3.7 V / 1.8 V | 2.2~3.7 V / 1.8 V |
| 升压 | **片内 booster 控制 + 外部功率级** | 同 | 同 |
| **GDR / RESE 脚** | ✅ 有（pin 2 / pin 3） | ✅ 有 | ✅ 有 |
| **24P 脚序** | 1 NC / 2 GDR / 3 RESE / 4 NC / 5 VSH2 / 6 TSCL / 7 TSDA / 8 BS1 / 9 BUSY / 10 RES# / 11 D/C# / 12 CS# / 13 SCL / 14 SDA / 15 VDDIO / 16 VCI / 17 VSS / 18 VDD / 19 VPP / 20 VSH1 / 21 VGH / 22 VSL / 23 VGL / 24 VCOM | **完全相同** | **完全相同** |
| SPI 写上限 | 20 MHz | 20 MHz | 20 MHz |
| VGH 范围 | +20 V typ | **10~20 V**（≤40 Vpp） | **10~20 V**（≤40 Vpp） |
| VSH1 / VSH2 / VSL | +15 V / +5 V / −15 V | VSH1 **8.6~17 V** | VSH1/VSH2 **2.4~17 V**；VSL −9~−17 V |
| 局刷 | 支持（含 OTP 波形） | 支持 | 支持 |

**三条直接结论**：
1. **电气接口对三者完全通用** —— 24 脚同序、供电同范围、SPI 同时序 ⇒ `J6` 插任一型号都不需要改板。
2. **三者都需要本板已有的那套外部泵**（L + NMOS + 3×MBR0530 + RESE + 储能电容）⇒ 本板的泵区**结构上适配全部三颗**。
3. **电压轨全部落在本板电容覆盖内**（本板各轨 1 µF/50 V，已知最高轨 VGH≈+22 V / VGL≈−20 V）。

### 15.2 ★ 真正的瓶颈：本板 `L1` 的电流能力（不是 IC，是电感）

| 项 | 原厂 SSD1680 要求 | **本板实装** | 判定 |
|---|---|---|---|
| 电感值 | 47 µH（CDRH2D18 / LDNP-470NC） | **68 µH**（CD32 类 3.5×3.0 mm，C2826627） | △ +45 %，闭环可适应 |
| **电流能力** | **Io = 500 mA (Max)**（= **温升**口径；官方实物 480 mA） | **额定电流 380 mA**（LCSC 口径；无独立 Isat，IDC 定义见 §12.7.6） | ⚠️ 约官方实物的 **79 %**（旧版"60 %"系口径混淆） |
| N-MOS | Si1304BDL / NX3008NBK（Rds(on) ≤ 2.1 Ω） | **AO3400A**（≈30 mΩ） | ✅ 远优（屏厂明确认可 AO3400 替代） |
| 二极管 | MBR0530（Io ≥ 500 mA） | MBR0530 ×3 | ✅ 一致 |
| RESE | 2.2 Ω / 1 % | 2.2 Ω ±1 % | ✅ 一致 |

**⇒ 按"温升额定"同一口径比，本板约为官方实物的 79 %**（换料后 98 %）。⚠️ **别把 Isat 当成"搬运能力上限"** —— Isat 管的是"会不会饱和"，不是"能搬多少"，见 §12.7.6「二次核查」。

**关键提醒**：这个短板**不能用减小 RESE 来补** —— 减小 RESE 会让峰值电流更大，反而让 `L1` **先饱和**（往危险方向）。

### 15.3 按面板尺寸的驱动能力判定

| 目标 IC（典型屏） | 面板容量（相对 2.7"） | 本板泵判定 |
|---|---|---|
| **SSD1680** @ 2.13"（250×122，手上 YMS122250） | ≈0.4× | ✅ **够用**（负载最轻） |
| **SSD1680** @ 2.7"（264×176，板上 J6） | 1.0× | △ **接近边界** —— 常温满电应可；**低温 / 低电量 / 满屏翻转**下可能建立变慢 |
| **SSD1683 / SSD1619A** @ 4.2"（400×300） | **≈2.6×** | ⚠️ **很可能不够** —— 峰值电流需求超过 `L1` 的 380 mA |

**判定依据**：面板负载（容性）随面积/像素数线性增长 ⇒ 所需的峰值电流上升，而 `L1` 的**饱和裕度**决定它还能撑多大负载。⚠️ 本条属**推理而非实测**（§12.7.6）。4.2" 400×300 的像素数是 2.7" 的 **2.6 倍**，对峰值电流的需求随之上升。

### 15.4 想驱动 4.2"（SSD1683 / SSD1619A）要改什么

| 层面 | 动作 |
|---|---|
| **L1** | 换成 **47 µH / Isat ≥ 1 A** 级别的功率电感（原厂参考件 CDRH2D18 是 4.5 mm 级；BOM 历史候选 SMMS0650 7.1×6.6×5 mm / Isat 2 A 曾因"过大"被弃） |
| **PCB** | ⚠️ 本板 `L1` 焊盘是 **3.5×3.0 mm**，换大封装电感**需要改布局** ⇒ 属 V2 范畴 |
| 其余 | Q1 / D3–D5 / R14 / 储能电容**均无需改**（见 15.2 表） |

### 15.5 两条容易踩的操作前提（屏厂/点墨官方口径）

1. **升压不会自动工作** —— 只有当 MCU 向 EPD IC **发出开启升压的指令**（`0x22`/`0x20` 序列并满足 HV ready）时泵才启动。固件必须先走完整 Power-On 序列，否则 VGH/VGL 一直是 0、屏全无反应（很容易被误判成"泵坏了"）。
2. **MOS 管过热的常见原因：二极管方向焊反 ⇒ MOS 短路**。本板 D3/D4/D5 方向已由网表逐脚实证正确，但**首件上电时仍建议先量 VGH/VGL**确认升压在 +20 V/−20 V 量级。

### 15.6 本板泵的实测验证清单（**尚未做过**）

板子还没打样，泵区虽已定为实焊，但**从未实测**（此前一直用 `J1` + 微雪驱动板模块，那是模块自带的泵）。首件建议按此顺序验：

1. 插 `J6` 裸屏 → 上电 → 下一帧全刷指令
2. **量 VGH ≈ +22 V、VGL ≈ −20 V**（万用表直流档，或示波器看建立过程）
3. 记录**实际刷新时间**，与规格书（2.7" 三色 15~17 s）比对 —— 明显变慢即泵能力不足
4. **摸 `L1` 温度** —— 发烫是饱和迹象（同时看画面是否出现条纹/对比度异常）
5. 若 2.7" 通过但 4.2" 不通过，即验证了 15.2 的 `L1` 短板结论

---

## 十六、屏尺寸选型：板面可用窗与候选屏（2026-09-20）

### 16.1 板面实测 —— 正面已被三条带"分完"

用活体 `pcb dump`（`Temp/r270_pcb_raw.txt`）的**器件焊盘外接矩形**算出（不用含位号文字的 bbox）：

| 带 | 纵向范围 | 高度 | 里面有什么 | 性质 |
|---|---|---|---|---|
| **天线净空带** | y ≥ 3259 mil | **481 mil = 12.2 mm** | U4 蓝牙天线本体（pads 到 y=3421）、**U13 SHT30**（y 3506~3628）、J1 排针（y 3432~3510） | **硬**：RF 净空不可覆盖；**SHT30 不能盖**（要测环境温度） |
| **屏区** | y 1448 ~ 3259 mil | **1811 mil = 46.0 mm** | 现用屏投影 70×46 mm；下面压着 U1(ESP32-S3)、R1/R5/R12/R18–R26、C11/C13/C19/C22/C36/C38/C40/C47/C49/C51 等 | 屏下只允许 **≤ 4 mm** 矮件 |
| **操作区** | y < 1448 mil | **1448 mil = 36.8 mm** | SW1 编码器 + 4 按键 + 3 LED + Type-C(J3/J4) + SD(J2) + TP1~TP6 | **硬**：面板上必须露出给人操作 |

横向：板宽 95 mm，现用屏只占 70 mm ⇒ **左右各余 12.5 mm（合计 25 mm）未利用**。

> ⇒ **屏可用窗 = 95 × 46 mm。纵向 0 余量，横向富余 25 mm。**
> 这也解释了现用屏外形 70.42 × 45.80 mm 为何"高度顶格"——当初就是按这条 46 mm 的带做的。

### 16.2 候选屏"装得下 / 装不下"（尺寸取官方规格）

| 型号 | 分辨率 | 有效显示区 mm | 外形 mm | 点距 mm | 判定 |
|---|---|---|---|---|---|
| 1.02" | 128×80 | 21.76 × 14.00 | 32.57 × 18.60 | — | ✓ 装得下但像素太少 |
| 1.54" / (B) | 200×200 | 27.60 × 27.60 | 37.32 × 31.80 | — | ✓ 太小 |
| 2.13" / (B) | 250×122 | 48.55 × 23.71 | 59.20 × 29.20 | — | ✓ 30,500 px |
| 2.66" | 296×152 | 60.088 × 30.704 | 83.5 × 37 | ≈0.203 | ✓ 44,992 px |
| **2.7" (B)（现用）** | **264×176** | 57.288 × 38.192 | **70.42 × 45.80** | **0.217** | ✓ **46,464 px ← 窗内最高** |
| 2.9" | 296×128 | 66.89 × 29.05 | 89.50 × 38 | ≈0.226 | ✓ 37,888 px（最宽，但只有 128 行） |
| 3.7" | 480×280 | 81.12 × 47.32 | **93.3 × 54.9** | 0.169 | ✗ **高 54.9 > 46，超 8.9 mm** |
| 4.2" | 400×300 | 84.8 × 63.6 | 91.0 × 77.0 | 0.212 | ✗ 高 77 > 46，超 31 mm |
| 4.2" (B) 三色 | 400×300 | 84.8 × 63.6 | 91.00 × 77.00 | 0.212 | ✗ 同上（且全刷 15 s、仅 1 bit） |
| 5.83" | 648×480 | 119.232 × 88.32 | 125.4 × 99.5 | 0.184 | ✗ 宽高双超 |

（官方来源：Waveshare 产品页 / Selection Guide / TME 目录，2026-09-20 取）

### 16.3 结论一 —— **2.7" 是当前板型的帕累托最优**

装得下的四款里（2.13" / 2.66" / 2.7" / 2.9"），**2.7" 像素最高（46,464）**。

- 2.9" 虽然更宽（外形 89.5 mm vs 70.4 mm），但**只有 128 行** ⇒ 总像素 37,888，反而**少 18%**。
- 2.66" 是"更宽更矮"的横幅形态（83.5 × 37），像素与 2.7" 基本持平（44,992），喜欢横向长条卡片可作备选。
- ⚠️ **要更大的屏，唯一的路是横向变宽**（纵向已经零余量）。而这一类里没有比 2.7" 像素更高的现货。

### 16.4 结论二 —— 真正该换的是"三色 → 黑白"，不是"变大"

| | 2.7" (B) 三色（现用） | **2.7" 黑白** |
|---|---|---|
| 分辨率 / 外形 / 脚序 | 264×176 / 70.42×45.80 / 24P | **完全相同** |
| 全刷 | 15~17 s | **3 s** |
| 快速刷新 | — | **1.5 s** |
| **局部刷新** | ❌ 不支持 | **0.42 s** |
| 灰度 | 2 级（1 bit，无灰阶） | **4 级** |

理由：这台设备显示的是**时间 / 天气 / 日程 / 温湿度 / 电量**——全是文字和图标，**红色几乎没有用武之地**；而"15~17 秒整屏重画"与"0.42 秒局部刷新"是**天壤之别**（能实时走秒、能做动画、能即时响应按键）。
代价：**同尺寸、同 24P 脚序、外壳不用改**（详见 §12）。

### 16.5 结论三 —— V2 若要大屏

| 方案 | 像素数（相对 2.7"） | DPI | 全刷 / 局刷 | 需要同时做的 |
|---|---|---|---|---|
| **3.7" 480×280 黑白** ⭐ | **134,400（2.9×）** | **150**（比现在锐 27%） | 3 s / 0.3 s | 换 `L1`（Isat ≥ 1 A，改布局）+ 外框加高 |
| 4.2" 400×300 黑白 | 120,000（2.6×） | 120（**与现在同粗**） | 5 s / 0.4 s | 同上，且面板更大更贵 |
| 5.83" 648×480 | 311,040（6.7×） | 138 | 5 s（黑白）/ **20 s（三色）** | 板变背板，外框约 150×110 |

**三条代价必须一起算：**

1. **泵负载 ∝ 有效区面积**：3.7" = 1.75×、4.2" = 2.47×、5.83" = 4.8×（基准 2.7" 的 2188 mm²）⇒ 受 `L1` 感量/饱和裕度限制（换料前额定 380 mA，**Isat 未公开**），**超过 2.7" 一律要换电感**（§15.2），而 `L1` 焊盘只有 3.5×3.0 mm ⇒ 必须改布局。
2. **刷新时间 ∝ 面板面积** ⇒ 屏越大越慢（不是越快）。
3. **外壳形态改变**：屏比 95×46 大之后，就**不能"骑在板上"**了，得让屏与板上下排列 ⇒ **外框高 ≈ 屏外形高 + 95 mm**（3.7" → 约 150 mm；4.2" → 约 172 mm；5.83" → 约 195 mm）。形态从"卡片摆件"变成"竖屏终端"，属 V2 产品决策。

⚠️ **红线：不要选"大屏 + 三色"。** 大屏的价值在像素与灰阶，三色把这两样都抵消掉了 —— 5.83" 三色全刷 **20 s**，比现用 2.7" 三色还慢。

### 16.7 2.7" / 2.9" / 3.7" 三款怎么选（2026-09-20）

**先纠正一个直觉：2.9" 不是 2.7" 的升级，是"换形状"。**

| 项 | 2.7" (B) 三色（现用） | **2.7" 黑白** | **2.9" 黑白 V2** | **3.7" 黑白** |
|---|---|---|---|---|
| 分辨率 | 264×176 | 264×176 | 296×128 | 480×280 |
| **像素数** | 46,464 | 46,464 | **37,888（−18%）** | **134,400（2.9×）** |
| 有效显示区 | 57.288×38.192 | 同 | 66.90×29.06 | 81.12×47.32 |
| **外形** | 70.42×45.80×0.98 | 同 | **89.50×38.00** | 93.3×54.9×0.78 |
| 点距 / DPI | 0.217 / 118 | 0.217 / 118 | **0.2265 / 112（最粗）** | **0.169 / 150（最锐）** |
| 长宽比 | 1.50（3:2） | 1.50 | **2.30（横幅）** | 1.71 |
| 灰度 | **1 bit** | 4 级 | 4 级 | 4 级 |
| 全刷 | **15~17 s** | **3 s** | 3 s | 3 s |
| 局部刷新 | **无** | **0.42 s** | 0.3~0.6 s | 0.3 s |
| **16px 中文容量** | 16 字 × 11 行 | 同 | **18 字 × 8 行** | **30 字 × 17 行** |
| 泵负载（面积比） | 1.00× | 1.00× | **0.89×** | **1.75×（须换 L1）** |
| 装进 95×46 窗 | ✅ | ✅ | ✅（左右仅剩 2.75 mm） | ❌ 超 8.9 mm |

> **2.9" 的真实定位**：宽 19 mm、矮 7.8 mm ⇒ "每行多 2 个字，但少 3 行"。它是**价签/公告栏那种"横向一条"的形态**，不是"内容更多的 2.7""。

**硬门槛：谁能直接插 `J6`**

| 型号 | FPC | 脚序与 2.7" 是否一致 | 结论 |
|---|---|---|---|
| 2.7" 黑白 | 24P / 0.5 mm | ✅ 完全一致 | **直接换，零改动** |
| 2.9" **V2** | 24P / 0.5 mm | ✅ **一致** —— 官方 V2 规格书 §1.5：1=NC、2=GDR、3=RESE、**4=NC**、**5=VSH2**、6=TSCL、7=TSDA、8=BS1、9=BUSY、10=RES#、11=D/C#、12=CS#、**13=SCL**、**14=SDA**、15=VDDIO、16=VCI、17=VSS、18=VDD、19=VPP、**20=VSH1**、**21=VGH**、22=VSL、**23=VGL**、24=VCOM | ✅ 可直插 |
| 2.9" **老版** | 24P / 0.5 mm | ❌ **不同**：**4=VGL、5=VGH**（20=VSH、13/14=D0/D1） | ❌ **不可插** —— 本板 `J6.4` = 空脚 ⇒ 该屏 VGL 会失去储能电容；`J6.5` = VSH2（挂 1 µF）⇒ 该屏 VGH 被并到我们的 VSH2 电容上。**必须买 V2 版** |
| 3.7" | 24P / 0.5 mm | ✅ 同族（SSD1680/1683/1619A 脚序一致，§15.1） | ⚠️ 电气可插，**物理装不进窗** |

**2.9" 的官方配套值与本板不同（列为"需首件实测"项）**

| | 官方 2.9" V2 参考电路 | 本板实装 |
|---|---|---|
| 升压电感 | **10 µH / 1 A** | 68 µH（2026-09-21 换料后 **47 µH / 470 mA**） |
| RESE | **3 Ω** | 2.2 Ω |
| 储能 | C2/C5 = 1 µF/50 V、C3 = 4.7 µF/50 V | 同为 1 µF/50 V（C26 = 4.7 µF/50 V） |
| MOS / 二极管 | Si1308EDL / MBR0530 ×3 | AO3400A / MBR0530 ×3 |

- 判断依据：2.9" 有效区 66.9×29.06 = **1943 mm²，只有 2.7" 的 0.89×**（负载更轻）⇒ 大概率能驱动；但官方给的是一套**"低电感 + 大电流"**的配套，与我们的**"高电感 + 小电流"**取向不同 ⇒ **属实测项，不属纸面保证**。
- **顺带解开上次那个实验的出处**：把驱动板 RESE 改到 **3 Ω** 而 2.13" 仍正常刷新 —— **3 Ω 正是 2.9" V2 的官方配套值**（见 §13.4）。也就是说那个值在这一族里是有官方依据的，不是随手试的。

**两个采购坑**

1. **2.9" 必须确认版本**：要 V2（或与 2.7" 同脚序），老版 24P 脚序不同、插上不可靠。
2. **`GDEW029T5D`（大连佳显，2.9" 主力型号）已 EOL 停产**，官方替代 = **`GDEY029T94`**；下单前确认型号。
3. ⚠️ 数据源陷阱：有分销页把 2.9" 的 **Dot pitch 写成 0.138 mm**，但 296 × 0.138 = 40.8 mm ≠ 66.89 mm（有效区宽）⇒ **该值错误**（0.138 实为 1.54" 面板的点距）。真实值见大连佳显规格书：**0.227 × 0.226 mm**。

**选择结论（按你要的形态定）**

| 你的目标 | 选 | 代价 |
|---|---|---|
| **在现板上把体验做上去**（推荐） | **2.7" 黑白** | 无 —— 同尺寸、同脚序、同外壳；15~17 s → 3 s、局刷 0.42 s、4 灰阶 |
| 想做"矮而宽的横向信息条"（价签/公告栏形态） | **2.9" 黑白 V2** | 像素 −18%、字更粗、行数 11 → 8；泵配套需实测 |
| 想要真正的大屏（趋势图 / 一周日历） | **3.7" 黑白** | 改外壳（外框 ≈105×150）+ 换 L1（Isat ≥1 A）+ 改布局 ⇒ **V2** |

### 16.6 三条尺寸红线（画外壳/选屏直接用）

```
1. 屏顶不得高于 y = 3259 mil（其上为天线净空 + U13 SHT30，不可覆盖）
2. 屏底不得低于 y = 1448 mil（其下为操作区，必须露在面板上）
3. 屏外形高 > 46 mm ⇒ 必然侵入上/下带之一 ⇒ 需重新分配正面分区（V2 机械决策）
横向：板宽 95 mm 是上限，现用屏仅占 70 mm，尚余 25 mm
```

## 十七、局刷 / 快刷能力由什么决定（2026-09-21）

> **一句话：由「面板颗粒体系 → 屏厂波形表（LUT）→ IC 机制」三层决定，而真正卡住三色屏的是第二层 —— 波形表。**

### 17.1 三层依赖（证据全部取自原厂手册）

**L1 面板颗粒体系** —— 决定"物理上能不能快"

- **黑白两态**：电泳颗粒只有黑白两组，状态少 ⇒ 可用短波形驱动。
- **黑白红三态**：多出红颗粒，**红颗粒达到饱和位置需要更长相位与更多重复** ⇒ 波形天生更长。

**L2 屏厂波形表（LUT，烧在面板 / IC 的 OTP）—— 决定性的一层**

- SSD1680 datasheet 特性表原文：**"Waveform can be stored in On-chip OTP or written by MCU"** —— 波形有两个来源：**屏厂烧进 OTP**，或 **MCU 经命令 `0x32` 写**。
- 实际出货是**屏厂把波形烧在 OTP 里**；屏规格书亦写明"电气/光学特性仅在厂家提供的 controller & waveform 下保证"（2.7" (B) 规格书 注 3）。
- **波形表如何决定时长**（datasheet §6.6 Programmable Waveform）：**12 groups × 4 phases = 48 phases**；每相位长度 **TP ∈ 0~255 frames**；每组可用 **RP / SR ∈ 1~256** 次重复；另有帧频 **FR**、全栅开启 **XON**、结束选项 **EOPT**。
  ⇒ **总时长 ≈ Σ(相位帧数 × 帧周期 × 重复次数)**，一张"局刷波形"就是把大多数相位设为 0（跳过）后的极短版本。
- **关键实证（全文检索）**：
  - **2.7" (B) 三色**规格书里 **"partial" 出现 0 次**，只给 **Full update 15~17 s**；
  - **2.7" 黑白**规格书给**三档**：**Full 3 s / Fast 1.5 s / Partial 0.42 s**，并说明过程差异 —— **全刷闪多次、快刷闪一次、局刷完全不闪**。
  - ⇒ **三色用不了局刷，不是 IC 不行，而是屏厂没为三色做局刷波形。**

**L3 驱动 IC 机制** —— 必要条件，但不是充分条件

- SSD1680 特性表就写着 **"Support display partial update"**；且**三色与黑白用的是同一颗 IC（SSD1680Z8）**。
- IC 提供的机制：
  - **窗口寻址**：`0x11`（RAM size）+ `0x44` / `0x45`（RAM X/Y 起止地址）⇒ 只更新指定矩形区域；
  - **波形选择**：`0x22` Display Update Control（选哪张 LUT）；
  - **波形可编程**：`0x32` Write LUT register，**153 字节**（VS[nX-LUTm] / TP / RP / SR / FR / XON），第 153 字节是 LUT 结束选项（对应寄存器 `0x3F`）；
  - **温度**：§6.8 **内部温度传感器（±2 °C，−25~50 °C）**可由命令选择，或经 I2C 单主机读**外部**温度传感器（即 FPC 的 TSCL / TSDA）；
  - `0x3C` 选择 VBD 边框波形。

### 17.2 LUT 是按"颜色状态对"组织的（datasheet Table 6-4 / 6-5）

| 显示类型 | R RAM | B/W RAM | 颜色 | 用哪张 LUT |
|---|---|---|---|---|
| **三色**（Table 6-4） | 0 | 0 / 1 | Black / White | **LUT0 / LUT1** |
| | 1 | 0 / 1 | **Red** | **LUT2**（LUT3 = LUT2） |
| **黑白**（Table 6-5） | 0 / 1 | 0 / 1 | Black / White | LUT0 / LUT1（**LUT2 = LUT0，LUT3 = LUT1**） |

⇒ 三色需要**三张独立波形**（红有专属波形），黑白只需两张且可复用 ⇒ **三色的波形表结构上就更"重"，屏厂没有余量为它再加一档短波形。**

### 17.3 能不能自己"加一档局刷"？—— 理论上能，实践上不行

- IC 开放 `0x32` 写 LUT（153 字节），**技术上有入口**；
- 但一份可用的局刷波形需要知道：颗粒迁移阈值、各级压的相位长度与重复次数、温度补偿曲线、DC 平衡约束 —— 这些数据在屏厂手里；写错会导致**残影、刷不干净，甚至损伤面板**。
- ⇒ 工程上把"支持哪些刷新档位"当作**屏的出厂属性**，不要指望从 MCU 侧造一档出来。

### 17.4 使用侧的两个约束（决定"能不能一直局刷"）

1. **残影累积**：局刷波形不让颗粒完全饱和 ⇒ DC 失衡与残影会累积，所以屏厂规定 **每 5 次局刷补一次全刷**（2.7" 黑白与 2.9" 规格书均写明）。
2. **温度**：局刷相位短、容错低 ⇒ 波形选择依赖温度；IC 自带内部温度传感器（±2 °C），因此 **TSCL / TSDA 悬空是常规做法**（本板 `J6` 的 6/7 脚即悬空，属正确接法）。

### 17.5 对照本项目的两块屏

| | 2.7" (B) 三色 | 2.7" 黑白 |
|---|---|---|
| 驱动 IC | SSD1680Z8 | 同族（命令集一致） |
| IC 是否支持局刷 | **支持** | **支持** |
| **屏厂给的波形档位** | **仅全刷 15~17 s** | **全刷 3 s / 快刷 1.5 s / 局刷 0.42 s** |
| 结果 | **用不了局刷** | **三档齐全** |

⇒ 换黑白之所以是"零硬件代价的最大体验提升"，根因就在这里：**IC 与脚序都不动，换的是那张波形表。**

**选屏时的可操作判据**：**不要问"用的是什么 IC"（SSD1680 两者相同），要问"规格书里有没有 Partial refresh 这一档、写的是多少秒"。**

## 附：与外壳/结构相关的三个尺寸（画外壳直接用）

```
屏模组外形   70.42 × 45.80 × 0.98 mm
有效显示区   57.288 × 38.192 mm
FPC          24 pin / 0.5 mm 间距（J6 位于板右侧带，插槽朝板边，需折 180° 插）
板上屏幕投影  x[492,3248] y[1448,3259] mil（另有支撑柱 5 mm）
屏下净空要求  投影区内只允许 ≤ 4 mm 高的器件
```

> 板框 95 × 95 mm R4；屏投影占板面中间约 70 × 46 mm。
